# 2026-09-23 工作日志

## 四大任务交付（新工具 / 代码审查 / SEO / 博客）

- **新增 11 个工具**（240 个 / 11 分类）：stopwatch、pomodoro-timer、break-even-calculator、
  roi-calculator、id-card-check、bank-card-validator、cidr-calculator、matrix-calculator、
  meta-tags-generator、bytes-size-converter、savings-goal-calculator。
  9 个由两个并行 agent 产出，2 个（bytes-size-converter、savings-goal-calculator）自写。
  changelog 已登记，`changelog-check` 240/240 通过。
- **代码审查**：两份报告 `docs/code-review-components.md`（27 条）、`docs/code-review-tools.md`（55 条），
  合计 82 条（P0 6 / P1 33 / P2 43）。P0 全部修复。
- **SEO**：问题页面 39 → **0**；HTML 平均 63.0 KB（雪碧图瘦身省 28.4 KB/页）。
- **博客**：新增契税新政长文 1 篇，修订 6 处过时内容（工具数 104/198 → 240 等）。
- 交付汇总：`docs/DELIVERY_2026-09-23.md`。

## 修复的 P0（都会影响线上）

1. **契税面积分档 90㎡ → 140㎡**（`src/lib/china-calc-extra.ts` + `src/tools/deed-tax/Tool.tsx`）。
   注意：算法在**两处各自硬编码**，改库不改 UI 等于没改。政策依据：财政部/税务总局/住建部
   2024 年第 16 号公告，2024-12-01 起执行。120㎡ 二套房 200 万差额 2 万元。
2. **计数接口**：ToolLayout / ClockImmersiveLayout 打向不存在的 `/api/counter` 且协议不符 →
   抽成 `src/components/CounterScript.astro` 共用，改用 `/api/count` + `{keys,delta}`。
3. **时钟页 toolId 占位注释**未替换（`const toolId = /* @json {toolId} */ ''`）。
4. **AdSlot 缺 data-ad-client / data-ad-slot** → 补到 `<ins>` 上（AdSense 只认 ins 上的属性）。
5. **mortgage.ts 零输入校验** → 加 `assertValidInput()`，调用方 `fund-loan-calc` 加 try/catch。
6. **46 个工具 icon 不存在** → 在 icons.ts 加 `ICON_ALIASES` + `resolveIcon()` 语义别名（43 条），
   并新增 `scripts/check-icons.mjs` 防复发（`npm run check:icons`）。

## 新增 / 重写的脚本

- `scripts/optimize-sprite.mjs`：产物级雪碧图瘦身，删每页用不到的 `<symbol>`，
  累计省 16.6 MB。护栏：检测到 JS 动态拼接 sprite id 的页面整页跳过。已挂进 `npm run build`。
- `scripts/gen-sitemap.mjs` 重写：lastmod 取真实更新时间（工具页 meta.updatedAt，内容页一次 git
  调用建表），priority 按工具权重；⚠️ 别对每个文件单独 execSync git，会拖到超时。
- `scripts/seo-audit.mjs`：修 ReferenceError（`source` 未定义）+ 灾难性回溯正则。
- `scripts/check-icons.mjs`：图标引用校验，已加 `npm run check:icons`。

## 本次踩到的坑（重要，可复用）

- ⚠️ **后台跑长命令不要用 `| tail` / `| head`**：管道在 head 读完提前关闭后会让整条后台任务
  永久挂起（实测 build 早已完成、任务却卡 14 分钟无输出）。**改成重定向到日志文件**（`> _build.log 2>&1`），
  再用 tail 读文件。判断「卡死」前先看日志时间戳，别被管道假象骗了。
- ⚠️ **正则灾难性回溯**：`(?:...|...|[^>])*?` 这类嵌套量词在 5KB 文本上就能跑 60 秒以上。
  把 `[^>]` 收紧成 `[^>"']` 让分支互斥，复杂度退化成线性。seo-audit 从 300s 超时 → 1.35s。
- build 真卡死时（非管道问题）仍按老办法：`mv .astro _astro_bak_<ts>` 后重跑。

## 待办（本轮遗留）


---

## 遗留问题收尾（同日下午，已全部完成并上线）

1. ⚠️ **发现并修复一起严重漏判**：昨天交付的 11 个新工具**全是死壳**。
   三件套（meta.ts / Tool.tsx / content.mdx）齐全、页面也构建出来了、看着像上线成功，
   但漏了第 4 步——`src/components/WidgetHost.astro` 里的水合注册。
   结果 11 个页面只有静态 HTML、没有一个 `astro-island`，点下去毫无反应，**而 build 不报任何错**。
   已补齐四处注册（import / `const isXxx = tool.id===...` / known 表达式 / load-idle-visible 三行渲染）。
   - 根因：我给两个并行 agent 的 prompt 里写死「只准创建 3 个文件、别碰其它文件」，把第 4 件挡在了门外。
   - 护栏：新增 `scripts/check-widgets.mjs` → `npm run check:widgets`，
     校验「未注册水合 / 幽灵注册 / Tool.tsx 缺默认导出」，240/240 通过。
     `npm run check:all` 一次跑完 widgets + icons + changelog 三项。
2. **水合策略 load → idle**：230 个工具改成 `client:idle`，首屏不再被几百个组件的水合 JS 阻塞。
   10 个时间敏感型保留 `load`：clock 分类 4 个 + countdown-timer、totp-generator、
   timestamp-converter、today-info、stopwatch、pomodoro-timer（TOTP 晚一秒就是错码）。
3. **时钟页备案补齐**：`ClockImmersiveLayout.astro` 底部新增精简备案条（ICP + 公安 + 站内链接），
   4 个全屏时钟页此前因隐藏 Footer 而无备案号。
4. **图标别名 → 真实 path**：35 个补上真实 Lucide path（数据源 lucide-static@1.47.0 unpkg 原文抓取，
   非手写）；5 个工具的引用方改成语义正确的真实图标（log/power-root/trig → `sigma`，
   prime-factor → `divide`，hourly-wage → `coins`）。字典 106 → 141 个真实图标，
   `ICON_ALIASES` 清空（保留空表作兜底通道）。
5. **仓库卫生**：清理 296M 垃圾（8 个 `dist_bak_*`）+ 9 个 `_astro_bak_*` + 76 个 `_*.log`。
   ⚠️ **重要修正：工作区（D 盘项目目录）里 `rm` 是能用的**，此前记忆里「本机 safe-delete 拦截 rm」
   只在个人目录（Desktop/Downloads/Documents）成立。已补 .gitignore 规则防止再生。
6. **已部署上线**：`bash deploy/nightly-2026-09-11-deploy.sh`（站点 587 页 + MCP 240 工具 + 重启）。
   服务器侧 `--resolve` 回环复验：首页 200、时钟页备案已出、秒表 astro-island 存在、idle 生效、抽样 6 页全 200。

## 收尾时的两个教训

- ⚠️ **验证「页面存在」不等于「功能可用」**：昨天我用 `dist/tools/xxx/index.html` 是否存在来判定
  11 个工具上线成功，这是错的。静态站点的组件必须显式注册水合岛屿，缺了页面照样生成、
  build 照样成功。判定新工具是否真上线，要查产物里的 `astro-island` 或 client 指令。
- ⚠️ **`git add -A` 会卷进垃圾**：本次误把 `_chrome_prof/`（177 个 Chrome 缓存文件）
  和 `tmp/` 加进了版本库，事后 `git rm -r --cached` + 补 .gitignore 才净化。
  批量提交前先 `git status --porcelain` 过一眼非源码目录。

## 代码推送（已完成）

- ✅ 已推送 GitHub：`808b681`（本轮修复）+ `8358bdd`（版本库净化），远端 `8358bdd` == 本地 HEAD。
- 过程：连续 4 次失败（`Connection was reset` / `Failed to connect to github.com:443 after 21s`），
  第 5 次成功。**GitHub 直连间歇性抽风，重试即可，别改代理和凭据**（用 GCM 缓存 token 的
  Basic auth 也会失败，说明与凭据无关）。总共失败 4 次才成，耐心比调参有用。
