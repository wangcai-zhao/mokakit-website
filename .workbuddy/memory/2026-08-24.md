# 2026-08-24 工作日志

## workbench.html 进度工作台刷新
- 任务：把 `https://www.mokakit.com/workbench.html` 刷新到当前真实状态（仅数据，不改版式；单文件直传，不整站 rebuild、不动 git）。
- 改 `workbench/status.json` 手动叙事：
  - deploy.domainStatus：mokakit.cn 2026-08-23 已批准并接入（DNS A + 证书 SAN + 整站 301 验证）
  - deploy.ssl：续至 2026-11-20（ECDSA）
  - deploy.mcpServer：9 tool → **15 tool（14 计算 + mokakit_search）**，catalog 104 元数据
  - milestones：mokakit.cn 接入 / MCP 同步 6 新工具 改 done（sitemap 提交仍 todo）
  - todos：对应两条 done；batches 补「第十六批·时钟栏目」(clock 4 工具)
- `npm run workbench` 重生成：工具 **104** / 分类 **10**（ls 见 105 目录，但其中 1 个无 meta.ts，生成器已过滤，与 memory 的 104 一致）。
- scp `public/workbench.html` → `root@58.87.68.151:/var/www/mokakit/workbench.html`（SSH config 已配 mokakit_deploy 密钥）。
- 线上校验通过：generatedAt=2026/8/24 21:29:10，字节 52955 与本地一致，关键文案全部更新，仅 sitemap 里程碑仍 todo。

## Bing 站点验证文件上线
- 需求：Bing Webmaster Tools 验证 mokakit.com 所有权。
- 源：`C:\Users\zhao-\Downloads\BingSiteAuth.xml`（令牌 `3321D7A2FBE040038504F90CDB7C4B61`）。
- 落站：先 `cp` 到 `public/BingSiteAuth.xml`（与 baidu_verify 同范式，后续 build 自动带），再 scp 直传 `root@58.87.68.151:/var/www/mokakit/BingSiteAuth.xml` 立即生效（用户要求直接传根目录保证可访问，跳过整站 deploy）。
- 线上校验：`curl --resolve www.mokakit.com:443:127.0.0.1 https://www.mokakit.com/BingSiteAuth.xml` → **HTTP 200**，内容正确。
- 注意：scp 直传的文件会在下次 `deploy.sh` 整站替换时被覆盖，但已同步进 `public/`，重建后仍在。

## sitemap 线上 404 修复
- 现象：用户要 sitemap 链接，发现 `https://www.mokakit.com/sitemap-index.xml` 返回 **404**，但 robots.txt 里指向它。
- 根因：本会话早前跑 `deploy.sh`（空输出中断）触发 `astro build` 清空 dist 后只建了 155/32 页（构建卡死 / 安全删除策略拦 `rm/mv` 清不掉 partial dist），sitemap 未生成也未上传；线上仍是用 2026-08-19 全量部署（整站完好，仅缺 sitemap）。
- 修法（不整站重建，避免再卡死）：用完好备份 `dist_bak_p3`（412 页 + sitemap 2 xml）的 `sitemap-0.xml`/`sitemap-index.xml` 直传 `root@58.87.68.151:/var/www/mokakit/`。
- 校验：两文件均 **HTTP 200**；sitemap-0.xml 含 410 条 URL，域名 www.mokakit.com，与线上页面集一致。
- 本地收尾：用 `cp -r dist_bak_p3/. dist/` + `cp public/BingSiteAuth.xml dist/` 还原本地 dist（412 页 + Bing + sitemap），避免下次 deploy 发布残缺站点。
- 经验：构建反复卡死时，优先从 `dist_bak_*` 取对应文件直传，而非硬刚整站 rebuild。
