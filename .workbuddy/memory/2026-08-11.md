# 2026-08-11 工作日志

## 刷新进度工作台（仪表盘）
- 旺财续干，先要求更新 `http://127.0.0.1:4680/workbench.html`（即 `dist/workbench.html`，预览端口实际读这份）。
- 重跑 `npm run workbench` 重新扫描代码 → 写入 `workbench/workbench.html`（内部版）+ `public/workbench.html`（公开脱敏版）；再 `cp public/workbench.html dist/workbench.html` 让预览端口立刻看到新快照。
- 刷新后实测规模（对比 8/9 日志的 69 工具 / 364 页）：
  - **工具总数 88**（8/9 后净增 19，对应 memory 中"时间日期类批量复刻 24 个净增 21"那批——可能含少量重叠扣除）
  - **全站页面 389、长尾子页 247**
  - **深度内容覆盖 31/88 = 35%**（8/9 时约 15%，六段式正文明显在补）
  - 单位换算 16 类 / 161 单位 / 213 长尾对；好站导航 **36 组 / 624 条**（8/9 前为 31 组 / 494 条）
  - 9 分类全非空；dist 快照 mtime 仍标 8/11 14:50（那是 sitemap mtime，非工作台）。
- 验证：预览端口返回 200，新文件 mtime 23:37，页面含 88 个工具 id。

## 待推进（下一棒，待旺财定方向）
- 战略主线仍是「先做中国本土计算器（个税/年终奖/社保公积金/房贷提前还款/公积金贷款/增值税）」，顺带抽纯 TS compute 层喂饱六段式 + MCP。
- 当前最大 SEO 缺口：深度内容仅 35% 覆盖；本土计算器是竞品（calculatorlib）完全盲区，搜索量高。

## 交付：中国本土计算器（第一批·个税 + 年终奖计税）
- 旺财选定「中国本土计算器」为主攻线。第一批做搜索量最高、共用税率表的 **个税 + 年终奖计税** 立样板。
- 抽共享纯 TS compute 层 `src/lib/china-tax.ts`：`ANNUAL_BRACKETS`（综合所得7级）、`MONTHLY_BONUS_BRACKETS`（年终奖月度表）、`BASIC_DEDUCTION_ANNUAL=60000`、`SPECIAL_ADDITION_REF`、`BONUS_BLIND_THRESHOLDS`；函数 `progressiveTax`/`bracketFor`/`calcIncomeTaxAnnual`/`calcBonusTaxSeparate`/`compareBonus`。decimal.js 保精度，未来 MCP 复用。
- 踩坑：年终奖税额 = `奖金×税率(由÷12定档) − 速算扣除`，初版误把 `bonus÷12` 当计税基数 → 税额错一个数量级；改 `bracketFor` 按 `÷12` 定档、全额奖金乘税率后修复。node 断言全绿（`scripts/test-china-tax.mjs`）。
- 新增 `src/tools/income-tax-cn/`（个税：meta+Tool+content 六段式）与 `src/tools/bonus-tax-cn/`（年终奖：税率盲区警示 + 单独vs并入对比）。
- WidgetHost.astro 接线：2 import + 2 布尔 + known 链补 `|| isIncomeTaxCn || isBonusTaxCn` + 2×3 渲染块（并行编辑曾竞争失败，已逐个补齐并 grep 核验四项齐全）。
- 构建：`dangerouslyDisableSandbox` + `NODE_OPTIONS="--require=.../noop-shim.cjs --use-system-ca --max-old-space-size=4096"` 跑 `npm run build` → 390 页 / sitemap 388 条。预览 4680 两工具 HTTP 200、标题正确、六段式+FAQ 已 SSR、无「开发中」占位。
- status.json：本土计算器里程碑改 doing(2026-08-11)、todo 标注个税/年终奖已上线、剩余 4 个待做。
- 工作台刷新：工具 90、页面 391、深度内容 33/90(37%)，两新工具入列。
- 剩余：社保公积金 / 房贷提前还款 / 公积金贷款 / 增值税（compute 层已就位，按同模板补齐即可）。

## 交付：中国本土计算器（第二批·剩余 4 个全上线）
- 旺财拍板「四个开干」。新增 4 个工具，每个 抽纯 TS compute 层 + 六段式 content.mdx + WidgetHost 接线：
  - `social-security-cn` 五险一金：`src/lib/china-social-security.ts`（`DEFAULT_SS_RATES` 全国通用费率、基数上下限夹取、个人/单位分项与汇总）。
  - `mortgage-early-repayment` 房贷提前还款 + `fund-loan-calc` 公积金贷款：共用 `src/lib/mortgage.ts`（`equalPaymentMonthly`/`buildSchedule`/`calcEarlyRepayment`，支持等额本息/本金 + 缩短期限/减少月供两种提前还款处理）。
  - `vat-calc` 增值税：`src/lib/china-vat.ts`（`calcGeneralVat` 一般计税含留抵、`calcSimpleVat` 简易计税、`excludeTax` 含税还原）。
  - compute 层与个税那批累计 4 个 lib 文件，未来 MCP Server 直接 import。
- 断言校验 `scripts/test-new-libs.mjs`：39 条全绿（社保分项、房贷月供 100万/3.85%/30年=4688.08、提前还款缩短期限省约 32.7 万、增值税留抵 11.7 万等）。
- WidgetHost 接线（串行 Edit，避免上次并行竞争）：4 import(90-93) + 4 布尔(174-177) + known 链末补 + 4×3 渲染块；grep 核验四项齐全。
- 构建：394 页 / sitemap 392 条。预览 4680 四工具 HTTP 200、标题正确、六段式+FAQ 已 SSR、零「开发中」占位。
- status.json：本土计算器里程碑 done、todo 标 done（6 个全部上线）。
- 工作台刷新：工具 **94**（90→94）、页面 **395**、深度内容 **37/94(39%)**、四新工具入列、预览 200。
- 至此「中国本土计算器」战略线 6/6 完成，是 calculib.com 完全盲区、搜索量高的差异化主场。下一步候选：六段式内容补全（仍 39% 覆盖）、MCP Server、分享/计数。
