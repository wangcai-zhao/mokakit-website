# 2026-08-31 工作日志

## WorkBuddy 使用技巧专栏（/tips/）上线就绪

为借 WorkBuddy 热度给 MokaKit 引流 + 赚官方邀请积分，新增独立内容专栏。

### 新增/改动文件
- `src/config/site.ts`：新增 `workbuddy` 配置块（inviteCode=`7a17nrwwpe`、activityUrl=`https://www.codebuddy.cn/events/invite`、inviteActive=true）；`WORKBUDDY` 派生对象提供 `inviteUrl`/`rulesCheckedAt`。注意域名是 **codebuddy.cn**（非 workbuddy.cn，同属腾讯）。
- `src/content.config.ts`：新增 `tips` collection（schema 含 series/order/difficulty/relatedTools/cover/author/featured/showInvite，draft 默认 true）。
- `src/components/InviteCta.astro`：邀请卡组件（full/compact 两态）。关键合规设计：rel=`nofollow noopener noreferrer sponsored`、不走 /go/ 中转、明示跳转 codebuddy.cn、利益披露、不写死积分数值、百度统计埋点 data-track=invite-cta。
- `src/components/TipCard.astro`：列表卡（含难度徽章）。
- `src/pages/tips/index.astro` + `src/pages/tips/[slug].astro`：列表页 + 详情页（详情页自动渲染关联工具内链卡 + 文末 InviteCta）。
- `src/components/icons.ts`：补 gift/megaphone/external-link 图标。
- `src/utils/jsonld.ts`：补 `articleLd`（详情页传 ogType=article）。
- `src/components/Header.astro`：桌面+移动端加「技巧」导航（分类从 4 降到 3 腾位）。
- `src/pages/index.astro`：中国本土计算器区后插入首页专栏卡区（取 featured 前 3 篇，无则整块不渲染）。
- `src/tools/skill-generator/content.mdx` + `src/tools/mcp-config-generator/content.mdx`：内嵌 `<InviteCta variant="compact" />`。
- `src/pages/agents/[id]/index.astro`：workbuddy-eco 分组加「技巧专栏」入口 + 文末 InviteCta。
- `src/components/Footer.astro`：底部加「使用技巧」链接。
- `scripts/new-tip.mjs`：草稿脚手架（校验 slug/工具 id，防 frontmatter 写错崩 build）。
- `scripts/topic-backlog.md`：15 个选题储备 + 写作约束 + 引流路径。

### 首批 4 篇已写（draft:false，前 3 篇 featured:true）
mcp-json-setup / skill-md-pitfalls / auto-weekly-report / batch-json-units。正文去 AI 味、自然植入 WorkBuddy 操作流、内链 MokaKit 工具、不写具体积分数字。

### 定时任务（automation_update）
- `automation-1788191652169` 每周二五产稿（rrule FREQ=WEEKLY;BYDAY=TU,FR）
- `automation-1788191652296` 每月 1 号复核邀请活动规则（rrule FREQ=MONTHLY;BYMONTHDAY=1）

### 验证
- `npm run check`：本次改动 14 个文件零新增错误（仅全站既有 z 弃用警告 + agents/index SITE 未用警告）。修复了 tips/[slug].astro 误从 registry 导入 ToolMeta 的报错（改从 @/tools/types 引入）。
- ⚠️ build 仍需**本机 Git Bash** 跑（沙箱内 astro build 会卡死在 16/446 页）；新页面自动进 sitemap。
- ⚠️ 新用户 2000 积分福利官方标注 8-31 截止，需稍后复核是否续期（已在 InviteCta 与 topic-backlog 留「以官方活动页为准」措辞）。
