# 2026-08-23 工作日志

## mokakit.cn ICP 备案获批
- 旺财确认 **mokakit.cn ICP 已获批，证号京ICP备2026051111号**（与 www.mokakit.com 同主体、同号）。
- 线上核验（ssh root@58.87.68.151）：
  - 证书 SAN 仅 `DNS:mokakit.com, DNS:www.mokakit.com`，**不含 mokakit.cn**（核心缺口：https://mokakit.cn 会弹证书告警）。
  - conf.d 仅 mokakit.conf（正式版，Aug14 更新）；/var/www/icp-pending 占位目录残留但未被 conf.d 引用（无害）。
  - nginx 80/443 的 .cn 跳转 server 块早已就绪（mokakit.conf:106 监听 mokakit.cn/www.mokakit.cn；mokakit-ssl.conf:113 同）。
- 代码侧 site.ts：`altDomains` 仍 `['https://mokakit.com']`，第10-11行注释还写"备案待批"。
- 接入三步走（详见 MEMORY.md 待办1）：
  ①【本地可做】site.ts 改 altDomains 加入 mokakit.cn（保留 mokakit.com）+ 修正"备案待批"注释，build 待命；
  ②【旺财操作】域名商加 DNS A：mokakit.cn / www.mokakit.cn → 58.87.68.151；
  ③【DNS 生效后 SSH】重签 LE 证书加 SAN（mokakit.cn + www.mokakit.cn），nginx -t && reload。
- 状态：备案已批、待接入。证书 SAN 缺口已固化进 MEMORY.md 生产环境备注。

## mokakit.cn 接入完成（续）
- 证书重签：`certbot certonly --webroot -w /var/www/mokakit --cert-name mokakit.com --expand -d mokakit.com -d www.mokakit.com -d mokakit.cn -d www.mokakit.cn`，成功；SAN 现含全部 4 域名，有效期延至 2026-11-20。
- 重签前置：给 nginx `mokakit.conf` 的 mokakit.cn 80 server 块加 `location /.well-known/acme-challenge/ { root /var/www/mokakit; }` 例外（否则整站 301 会让 ACME 挑战文件取不到）；已同步 deploy/nginx/mokakit.conf 并 scp 上传 + reload。
- 端到端验证：http://mokakit.cn:80 → 301 → https://www.mokakit.com/；https://mokakit.cn:443 与 https://www.mokakit.cn:443 均 301 到 www，**curl 无 SSL/error 输出（证书告警消除）**。mokakit.cn 接入完成。
- 本地 site.ts 已改 altDomains 加入 mokakit.cn + 修正"备案待批"注释（代码就绪），但 git PAT 失效 push 阻断，待 PAT 恢复后构建部署（不影响 mokakit.cn 跳转，跳转在 nginx 层）。
