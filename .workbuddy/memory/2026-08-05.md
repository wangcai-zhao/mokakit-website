# 2026-08-05 工作日志

## MokaKit 迭代体检（代码全面审计）
- 结论：核心架构健康（67 工具接线一致、自动注册表 OK），无需重构。
- 高危项（已亲自核实）：
  1. `public/workbench.html` 泄露内网 IP(58.87.68.151)/真实姓名/备份路径/ICP 状态 → 待脱敏 + noindex + robots 屏蔽
  2. `/search/` 不存在，但首页/Header/404 的搜索表单全链向它（死链）
  3. `/about/` `/privacy/` 不存在，页脚链接 404
  4. `scripts/gen-sitemap.mjs` 缺 `lastmod`
- 中低项：图标 SVG 重复膨胀(首页 101KB/列表 120KB)、52 处复制逻辑重复、9 工具缺 a11y label、`markdown-preview` 残留 console.log + 暗色硬编码色、`@preact/signals` 死依赖、好站导航 15 重复 URL、根目录 23 临时文件、`og:image` 缺失、57 岛屿全 client:load、汇率/AI价格/GitHub Stars 为静态快照。
- 规划四阶段：P0 止血 → P1 刚需/性能 → P2 提质 → P3 增长上线。本轮待旺财确认推进范围。
