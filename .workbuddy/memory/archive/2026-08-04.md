# 2026-08-04

## MokaKit 项目 9 点自动跟进（automation-1785784928861）
- 核对状态：7 工具全落地、deploy 资产就绪、OpenClaw 同机共存方案就绪、仅本地预览未上云。
- 品牌已统一 MokaKit / 摩卡工具箱；导航无 /sites；摩卡配色已应用未确认。
- 产出跟进文档 `.workbuddy/automations/automation-1785784928861/status-2026-08-04.md`。
- 待旺财拍板：① 腾讯云公网 IP ② /sites 导航+品牌定稿 ③ 摩卡配色 ④ 第三批工具。
- 提示：SITE.description 文案含未做工具（简繁/汇率），与内容不符。

## 旺财 9:18 跟进：sites + 免费 SSL + 第三批工具（实做）

- 旺财确认：① /sites 板块要做 ② 摩卡色暂不满意但先不动（记录：后续可微调）③ 第三批开始铺 ④ 需支持 SSL，前期用免费证书。
- 域名状态：已注册 + 实名认证完成，正在腾讯云提交 ICP 备案，预计 7 个工作日完成。备案期域名暂不要解析到国内服务器对外提供内容，仍走 icp-pending 占位页。

### /sites 导航板块（已落地，纯静态数据驱动）
- 新增 `src/data/sites.ts`（分类+外链，uniform nofollow）、`src/pages/sites/index.astro`（卡片网格，外链 rel=noopener noreferrer nofollow）、Header 加"好站导航"入口。
- **不引入 WordPress / PHP / MySQL**，@astrojs/sitemap 自动收录 /sites/。

### 免费 SSL 方案（已落地）
- 新增 `deploy/nginx/mokakit-ssl.conf`（443 完整配置，证书路径统一 /etc/nginx/ssl/mokakit.com/fullchain.cer + mokakit.com.key）。
- `mokakit.conf` 第4段改成「include mokakit-ssl.conf」方案。
- `server-setup.sh` 新增 `--cert`（acme.sh 申请 Let's Encrypt 免费证书，90天自动续期）与 `--enable-ssl`（自动取消注释+reload）。备案通过+域名解析后才执行。

### 第三批工具（已落地，5 个）
- url-encoder（URL编解码/dev）、base64（Base64编解码/dev，UTF-8 正确处理）、uuid-generator（UUID v4/dev，crypto.getRandomValues）、text-counter（字数统计/text）、color-converter（HEX/RGB/HSL 互转/convert）。
- 全部 meta.ts+Tool.tsx 并在 WidgetHost 注册；Icon 新增 compass/link/file-code/align-left/palette。
- 构建：62 → **68 页**，无错误、无 example.com 泄漏，sitemap 收录新页。

### 架构决策（回答旺财：管理后台 / WordPress）
- **保持纯静态，不引入 WordPress**。理由：① 破坏纯静态架构（增 PHP/MySQL，内存数百 MB，与 2核2G 共存 OpenClaw 冲突）② WP 是头号被黑目标，安全面大增 ③ 工具站内容更新低频，数据文件驱动足够 ④ 与一键 deploy 流程不搭。
- 若将来要用户投稿：静态表单 + 无服务器(Serverless) 收集，人工并入数据文件；若要可视化编辑：headless CMS（Decap / 飞书多维表 / Notion 同步）仍生成静态。sites 板块同理用 src/data/sites.ts 维护。

### 待办（未阻塞）
- 备案通过 → `deploy/deploy.sh root@IP --setup`（占位）→ 防火墙放通 80/443 → A 记录 → `--live` → `server-setup.sh --cert` → `--enable-ssl`。
- SITE.description 仍提"简繁转换、汇率换算"等未做工具，需改文案或补齐对应工具。
- 摩卡配色后续可按旺财意见微调。
- 预览：http://localhost:5182/（dist 已含 12 工具 + sites 板块）。

### 部署推进 09:53（旺财给 IP 58.87.68.151）
- SSH 探测：`ssh -o BatchMode root@58.87.68.151` 失败 → ① host key 与本地旧记录不符（疑似服务器重装过系统）② 无可用密钥/密码认证。
- 旺财确认：是自家腾讯云轻量服务器，走密钥登录。
- 已本机生成部署密钥对：`/c/Users/zhao-/.ssh/mokakit_deploy`（私钥，不外传）+ `.pub`（公钥已给出，待用户导入腾讯云并绑定实例）。
- 已 `ssh-keygen -R 58.87.68.151` 清理旧 host key 记录。
- 下一步：用户去腾讯云控制台「密钥」导入公钥并绑定实例 → 完成后我测连通并跑 `deploy/deploy.sh root@58.87.68.151 --setup`（仅占位页，备案中不上 --live）。
- 备案期提醒：域名 A 记录现在可指到 58.87.68.151（占位页合规）；防火墙放通 80/443。

## 10:13 旺财决策：重置 58.87.68.151（含重装系统），OpenClaw 不要了
- 旺财确认：把当前轻量服务器**整台重置（含重装系统）**跑 MokaKit；OpenClaw 明确不需要。可选：仅保留其用户文档/图片。
- **能力确认**：该机是标准 KVM 云虚拟机，腾讯云控制台「重置系统」支持 Ubuntu24.04/Debian12/TencentOS；重装后 IP 不变(58.87.68.151)，腾讯云 agent 自动回来。推荐重置选 **Ubuntu 24.04 LTS**（server-setup.sh 已验证）。
- **wenyan-mcp 真相**：只是 nginx 反代 3000→127.0.0.1:3001（带 API key `liuruyan2026`）；后端(3001)当前未运行，无重要数据，随重装消失。
- **OpenClaw 可迁移性（供参考，未采用）**：数据全在 `~/.openclaw`(1.5GB)，npm global `openclaw`+`openclaw-setup`，systemd `openclaw-upgrade-guard.service` 自启。新机装 Node22+openclaw→拷 ~/.openclaw 即可复活（重，故不迁）。

## 备份落点（本机 C:\Users\zhao-\mokakit-backup\）
- 用户自有文件：`feishu_bot_creator.py`(61KB,1593行)、`ubuntu/`、`lighthouse/`、`wenyan-mcp.conf`。
- OpenClaw 文档/图片（排除浏览器缓存）：`openclaw-docs-imgs/` = **240 文件 / 134MB**，分布 agents(智能小秘书 3)/canvas(1)/extensions(71,如 ddingtalk/openclaw-lark/memory-tencentdb)/media(27,45M)/workspace(138,85M)；类型 53 图 + 186 文档。
- 服务器 /tmp 临时分片已清理。

## 沙箱传输坑（重要，可复用）
- 本 Bash 环境：**scp 远程通配符失败**（`/tmp/oc_chunk_*` 整命令被掐）；**单次大文件传输(121MB)整条命令被静默掐断**。
- 解决：① 显式文件名、绝不远程通配符；② `ssh host "cat 文件" > 本地` 管道方式（5MB 单片稳定）；③ 大文件切 5MB 分片逐片 ssh-cat；④ 单命令内连续 ssh 连接过多也会断，分多批补拉。

## server-setup.sh 修复（避免重置后 --setup 再翻车）
- 原「禁用默认站点」仅 `mv sites-enabled/default sites-enabled/default.disabled-by-mokakit`——**仍在 sites-enabled 被 include，与我们的 default_server 冲突 → nginx -t 失败**（此前已因此翻过一次）。
- 改为：移出被 include 目录 → `mv sites-enabled/default sites-available/default.disabled-by-mokakit`；conf.d/default.conf 改名 `.disabled-by-mokakit`（不匹配 *.conf）。已 `bash -n` 通过。
- 顶部「与 OpenClaw 共存」注释改为通用「不碰其他服务/非80-443端口」。

## 下一步（等旺财控制台重置）
1. 腾讯云控制台 → 轻量服务器 58.87.68.151 → **重置系统** → 系统镜像 **Ubuntu 24.04 LTS**
2. 登录方式：**绑定 mokakit_deploy 密钥**（公钥 `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIC9kaI6kuru3tbQllBvHQ/6Shy7TNHX9cDpjCbpvhjuD mokakit-deploy`；若未导入密钥控制台，先「创建密钥/导入公钥」再绑定；或设 root 密码后我 WebShell 加回公钥）
3. 重置完成回「好了」→ 我清 known_hosts + 跑 `deploy/deploy.sh root@58.87.68.151 --setup`（仅占位页，备案中不上 --live）
4. 防火墙放通 80/443；域名 A 记录指到本机（占位页合规）
5. 备案通过 → `--live` → `server-setup.sh --cert` → `--enable-ssl`

## 11:14 确认 .md/.epub（不局限 openclaw）+ 补备 openclaw 之外用户内容
- 旺财回忆：之前 OpenClaw 帮他生成过 .md/.epub，要求全盘确认（不限 openclaw 目录）。
- 全盘结果：
  - **用户生成的 .epub**：`/root/.openclaw/workspace/xianyu-ops/中医体质养生指南.epub`(19.6MB) + 配套 `.md`/`.pdf`/`.pdf_32k`/`32开样品.pdf`/`make_epub_content.txt`。**这份 epub/md/pdf 已在之前 openclaw-docs-imgs 备份里**（路径 `openclaw-docs-imgs/workspace/xianyu-ops/`）。
  - `/usr/share/calibre/quick_start/*.epub` 是 calibre 软件自带示例（德/土/法/英等），非用户内容。
  - openclaw 目录之外无用户生成文档；`/root/.nvm/...`、`/root/.local/.../.pnpm` 的 .md 全是依赖库 README/CHANGELOG，非用户内容。
  - `/home/lighthouse`、`/home/ubuntu` 仅默认点文件（.bashrc 等），无用户内容。
- **之前漏备的 openclaw 之外用户内容**，本次补备到 `mokakit-backup/root-extras/`（57KB，ssh-cat 直传稳）：
  - `/root/skills/web-tools-guide/`（旺财自己写的 OpenClaw skill：SKILL.md、references/web-search-config.md、opencli-guide.md、setup-opencli.sh 等）
  - `/root/token_usage_report.py`、`/root/upgrade.sh`、`/root/.skillhub/`(config/metadata/version.json + skills_upgrade.py)、`/root/package.json`
  - 排除 `/root/node_modules`(依赖)。
- 结论：重置前**用户数据已全部安全落地本机**（feishu 脚本 + ubuntu/lighthouse + wenyan 配置 + openclaw 文档图片 134MB + root-extras 57KB）。可放心去控制台重置。

### 12:16 备案网站名称误填「摩卡工具」（品牌为「摩卡工具箱」）
- 用户提交 ICP 备案时网站名称填了「摩卡工具」，与代码/品牌 `nameCn: '摩卡工具箱'`（src/config/site.ts:24）差一个"箱"字。
- 评估：「摩卡工具」作为个人工具站备案名**合规、不违规**，仅与品牌名不一致；若备案仍在腾讯云初审可撤回改回「摩卡工具箱」，若已提交管局则接受即可，不影响上线。
- 代码无需改（品牌名保持摩卡工具箱）。备案通过后 SITE.icp 照填，页脚展示按备案名即可。
- 用户已决定：去腾讯云备案后台把网站名称改回「摩卡工具箱」（判断仍在初审阶段）。改完告知即可，我无需动代码。
- **已完成**：12:37 用户确认已将备案网站名称改回「摩卡工具箱」，品牌/备案名已统一。待备案通过后给备案号填 SITE.icp。

### 12:48 第四批工具铺量（备案等待期本地开发）
- 新增 5 个工具（源码 src/tools/ 下 meta.ts + Tool.tsx，纯本地计算/daisyUI 风格）：
  1. jian-fan-convert 简繁转换（text，内置约280对常用简繁差异字表，纯JS）
  2. currency-convert 汇率换算（convert，内置12币种参考汇率+可自定义，纯JS）
  3. regex-tester 正则测试（dev，JS RegExp 实时匹配/捕获组高亮，纯JS）
  4. qrcode-generator 二维码生成（dev，动态 import('qrcode') 库，下载PNG）
  5. markdown-preview Markdown预览（text，import { marked }，内置 .md-body 基础排版样式）
- 依赖：安装 qrcode + marked 到项目（托管 node npm-cli.js install，INSTALL_EXIT=0）。
- Icon.astro 补 6 图标：languages/scale/regex/qr/file-text + 之前已有。
- WidgetHost.astro 注册 5 个新工具（import + 布尔 + load/idle/visible 三档）。
- SITE.description 原提及「简繁转换、汇率换算」现已补齐，文案与内容一致。
- 构建：68 → **73 页**，BUILD_EXIT=0，无 example.com 泄漏，sitemap 全收录。
- 本地预览：python -m http.server 5183 --directory dist（首页/工具页 HTTP 200）。
- 当前工具总数：**17 个**（密码/单位换算 + 时间戳/JSON/大小写/进制/哈希 + URL/Base64/UUID/字数/颜色 + 本批5个）。

## 13:09 第五批：AI / WorkBuddy 工具「双子星」（旺财定的方向）
- 旺财："做一些 agent 或跟 ai 有关的工具，也可以做 workbuddy 的工具"。探讨后拍板：**先做 WorkBuddy 双子星（SKILL.md 生成器 + MCP 配置生成器），后端暂不碰（暂不做 serverless）**。
- 新增分类 `ai`（AI 与智能体，src/config/categories.ts，order:6，icon: bot），首页/分类页/导航自动出现。
- **skill-generator**（SKILL.md 生成器，ai）：填 名称/简介/触发场景/详细步骤 → 生成标准 SKILL.md（frontmatter name+description + 正文），复制/下载。
- **mcp-config-generator**（MCP 配置生成器，ai）：选预设（filesystem/github/fetch/puppeteer/sequentialthinking/自定义 stdio）→ 生成 mcp.json 的 mcpServers 片段，复制/下载。
- Icon.astro 补 `bot`(技能) + `plug`(MCP) 两图标。
- WidgetHost.astro 注册 2 个新工具（import + 布尔 isSkill/isMcp + load/idle/visible 三档）。
- 无新依赖（纯前端，无外部 API）。构建：73 → **76 页**，BUILD_EXIT=0，无 example.com 泄漏，sitemap 收录。
- 本地预览：`npm run preview -- --port 5183 --host 127.0.0.1`（astro preview，工具页/AI分类页 HTTP 200）。
- 当前工具总数：**19 个**（新增 2 个 AI 类）。
- 后续可选（琥珀档，需后端，暂不做）：AI 对话舱 / 工作流编排 / 真实 Agent 执行 / 知识库检索；以及纯前端仍可补：Token 计算器、Prompt 变量模板、提示词收藏库。

## 13:23 第六批：绿色零后端 AI 工具补齐（旺财："绿色零后端立即上线"）
- 在第五批「双子星」基础上，补齐 3 个纯前端 AI 工具，全部归 `ai` 分类：
- **token-counter**（Token 计算器）：动态 `import('gpt-tokenizer')`（cl100k_base），本地分词估算 GPT/4o token 数 + 字符数 + 每字符 token 比；装依赖 `gpt-tokenizer`（31 包，耗时约 2 分钟，INSTALL_OK）。
- **prompt-template**（Prompt 变量模板）：模板 `{{变量}}` + 变量 KEY=VALUE 行 → 实时渲染，未替换变量高亮提醒，复制结果。
- **prompt-library**（提示词收藏库）：localStorage CRUD（标题/内容/标签），搜索/复制/删除，数据仅存本机。
- Icon.astro 补 `hash`(Token) + `bookmark`(收藏库)；WidgetHost.astro 注册 3 个新工具（import + 布尔 isTok/isPT/isPL + 三档水合）。
- 构建：76 → **79 页**，BUILD_EXIT=0，无 example.com 泄漏，sitemap 收录。
- 本地预览：`npm run preview -- --port 5184 --host 127.0.0.1`（task mNVfyN），3 工具页 + AI 分类页均 HTTP 200。
- 当前工具总数：**22 个**（新增 3 个 AI 类，AI 分类共 5 个）。
- 注：生产正式发布仍待 ICP 备案通过；本次仅做进站点、本地预览可见，合规占位期不推生产。

## 13:54 第七批：提示词收藏库增强 + 提示词随机生成（旺财："增加分享按钮，再增加随机生成仅供娱乐"）
- **prompt-library 增强（分享/导入导出，纯前端离线）**：
  - 每条新增「下载.md」「分享码」按钮；顶部新增「粘贴分享码/导入」「导出全部JSON」「导入文件」。
  - 分享码 = base64(JSON)（`TextEncoder`+`btoa`），对方粘贴还原（`atob`+`TextDecoder`）；支持单条(item)与批量(items)两种载荷。
  - 导入文件读 `.json`（数组或 `{items}`），导出全部为 `mokakit-prompts.json`。
  - Toast 提示 + 顶部「仅供娱乐」提示，文案与图标一致。
- **prompt-dice 新增工具（提示词随机生成，仅供娱乐）**：归 `fun` 分类（icon: dice，lucide dice-5）。
  - 五大维度随机：角色/任务/格式/语气/约束，拼接成一句提示词；「再抛一次」重抽、「复制提示词」。
  - 明确标注「仅供娱乐与测试」，FAQ 声明非严肃用途。
- Icon.astro 补 `dice` 图标；WidgetHost.astro 注册 prompt-dice（import + 布尔 isPD + load/idle/visible 三档）。
- 构建：79 → **81 页**（新增 prompt-dice 工具页 + fun 分类页），BUILD_EXIT=0，无 example.com 泄漏。
- 本地预览：`npm run preview -- --port 5185 --host 127.0.0.1`（task MRQJ38），prompt-dice/prompt-library/fun/ai 分类页均 HTTP 200。
- 当前工具总数：**23 个**（fun 新增 1；AI 类维持 5 个）。绿色零后端 AI/娱乐工具基本铺满。
- 后续可聊：琥珀档（需后端的真·Agent/AI对话舱）、摩卡配色打磨、SEO/投稿表单；或继续扩 fun/life 等分类。

## 14:12 新方向探讨：复刻 creaibo.com/modelcost（Pricetag · AI 价格比价）
- 用户让分析该站能否复刻。结论：**前端与计算逻辑 100% 可用 MokaKit 栈复刻**（Astro+Preact+daisyUI，加一个 src/tools/<id>/ 即可自动注册）。
- 该站本质：AI 视频/生图/Token 多平台比价聚合器，6 模块（价格排行/场景筛选/白嫖额度/订阅摊薄/价格外维度/API 比价），按 CNY 归一化。
- **唯一真难点 = 价格数据维护**（creaibo 所谓「实时」也是定时人工校对，非实时 API；非技术门槛）。
- 落地建议（待旺财拍板）：① 范围先 LLM Token 比价，再扩视频/生图 ② 数据用仓库内置 prices.json（带 lastVerified，人工维护，先 20-30 条）③ 作为 MokaKit 新工具（ai 分类），随现有 deploy 上线。
- 当前站点已有 ai 分类 + token-counter，加比价工具框架零成本。工作量≈1-2天。
- 状态：仅分析，未动手；等旺财定方向（范围/数据源/放哪）。

## 14:00 第八批：四个 AI 工具深耕（旺财："这几个小工具都可以再深耕一下功能"）
- **prompt-template** 增强：①内置 6 个预设模板（角色扮演/系统提示词/Few-shot/翻译/代码审查/SEO）下拉一键载入；②「我的模板」localStorage 保存/载入/删除，复用提示词模板。
- **token-counter** 增强：①多编码切换 cl100k/o200k/p50k/r50k（对应 GPT-4o/4o-mini/3.5-Instruct/GPT-3），动态 import 子路径；②价格估算（模型预设单价 ¥/1M，输入+输出实时算花费）；③字符细分（中文字符/英文词/标点/行数）。
- **skill-generator** 增强：①扩展字段（参数/示例/注意事项/依赖）；②导入已有 SKILL.md 反向解析回填（frontmatter + 各 ## section）。
- **mcp-config-generator** 增强：①多 server 组合成一个完整 mcp.json（可增删多个 server）；②预设扩充至 13 个（新增 Notion/PostgreSQL/Slack/Brave/GDrive/Memory/Time）。
- 4 个 meta.ts 描述/keywords 同步更新。
- ⚠️ 构建踩坑修复（重要，易复现）：gpt-tokenizer `exports` 用 `"./*": { "import": "./esm/*.js" }`，通配符 `*` **不匹配 `/`**，致 `gpt-tokenizer/encoding/cl100k_base` 子路径在 Rolldown/ESM 下解析失败；Node `require` 会回退物理目录故本地 `node -e require(...)` 测试通过，但 `astro build` 失败（首次成功是依赖旧 .astro 缓存）。修复：astro.config.mjs `vite.resolve.alias` 把 4 个编码子路径精确映射到 `node_modules/gpt-tokenizer/esm/encoding/<name>.js`（用 `fileURLToPath(new URL('./node_modules/...', import.meta.url))` 绝对路径）。注意 astro.config.**mjs 是纯 JS 不支持 TS 类型注解**，初版 `(n: string) =>` 致 parse 失败，已改 `(n) =>`。
- 构建：81 页，BUILD_EXIT=0，无 example.com 泄漏。预览 5187 四页均 HTTP 200。本轮为「增强既有工具」，工具总数维持 **23 个** 未新增。

## 合并：提示词收藏库 + 提示词随机生成（旺财："两个工具合并，随机生成多加自选表单"）
- 把 `prompt-dice`（提示词随机生成）合并进 `prompt-library`，删除 `src/tools/prompt-dice` 目录；`prompt-library` 改名为「提示词工坊」（id 不变 `prompt-library`，URL 不变，category 维持 `ai`）。
- 单页双 Tab：①「我的收藏」（原收藏库全部：CRUD/搜索/分享码/导入导出）②「🎲 随机生成」（原随机生成增强：5 维度 角色/任务/格式/语气/约束 各自「启用开关 + 候选文本域(每行一个可增删)」+ 主题关键词注入；结果可复制/保存到收藏库）。
- WidgetHost.astro 移除 `isPD`/PromptDice 的 import 与三档水合块；prompt-library 现单工具承载双功能。
- 关键链：删 prompt-dice 后 `fun` 分类自动空（getStaticPaths 仅生成「有工具的分类」），`/tools/c/fun/` 不再生成(404 符合预期)。
- 构建：先 `rm -rf .astro` 重建 81→79 页，但旧 `dist` 未清理致 prompt-dice/fun 残留；后 `rm -rf dist` 彻底重建确认无残留、无 example.com 泄漏。工具总数 23→**22 个**（合并减 1）。

## 第九批：参考 GitHub it-tools 铺 6 个纯前端工具（旺财："再铺一批工具，再github上看看有没有"）
- 调研：WebSearch 摸了 it-tools(CorentinTh/it-tools) 等纯前端工具集合标杆，对照现有 22 工具筛出 6 个未覆盖、纯前端、零/低依赖点子。
- 新增 6 工具（id / 分类 / 依赖）：
  - `jwt-decoder` JWT解析器 / dev / 零依赖（base64url 解码三段，显示 header/payload，不验签，exp/iat 转可读时间）
  - `password-strength` 密码强度分析 / security / 零依赖（长度/字符类/熵/常见弱口令/连续序列/重复 评分+建议）
  - `rsa-keygen` RSA密钥对生成 / security / 浏览器 WebCrypto（2048/4096，导出 SPKI/PKCS#8 PEM）
  - `text-diff` 文本比较Diff / text / 零依赖（行级 LCS，增删行高亮）
  - `http-status` HTTP状态码速查 / dev / 零依赖（静态表 1xx-5xx + 搜索/分类筛选）
  - `cron-parser` Cron表达式解读 / dev / 零依赖（5段 *,-/ 翻译中文化描述）
- Icon.astro 补 `server` + `git-compare`（其余 key/shield/fingerprint/align-left/clock 已存在）。
- WidgetHost.astro 注册 6 工具（isJwt/isPwd/isRsa/isDiff/isHttp/isCron + 三档水合）。
- 构建：79 → **85 页**，BUILD_EXIT=0，无 example.com 泄漏。预览 5188 六页 + ai/dev 分类页均 200，fun 404（符合预期）。
- 当前工具总数：**28 个**。绿色零后端纯前端工具已基本铺满。
- 注：生产发布仍待 ICP 备案通过；本次仅本地预览可见，合规占位期不推生产。
- 待办（用户之前探讨过未落地）：① 复刻 creaibo AI 价格比价工具（需维护价格数据）② 琥珀档（需后端的真·AI对话舱）③ 摩卡配色打磨 ④ SEO/投稿表单。

## 第十批：UDI 码生成 + UDI 码解码（旺财："添加两个工具：UDI码生成要多种格式二维码+多行条形码、UDI码解码解析多种格式文本"）
- 新增 `barcode`（条码与标识）分类（slug barcode，icon: barcode，order 6；ai/life/fun 顺延为 7/8/9）。Icon.astro 补 `barcode` + `scan` 图标。
- **udi-generator UDI码生成**：
  - 字段表单：DI/GTIN(01)、批号(10)、序列号(21)、生产日期(11)、失效日期(17)、保质期(15)、数量(30)；实时组合 GS1 括号元素字符串 `(01)…(17)…`（可手动编辑，「用字段重排」按钮覆盖）。
  - 码类型 7 种可选：二维码组 QR Code / GS1 QR / Data Matrix / GS1 Data Matrix；条形码组 GS1-128 / Code 128 / Code 39。每当选中即渲染 canvas + 下载 PNG。
  - 依赖 `bwip-js` 4.11.2（npm 安装；动态 import('bwip-js') 按需加载，浏览器版自包含，toCanvas 绘制）。gs1-128 / gs1datamatrix 强制 GS1 括号 AI 格式，正合 UDI 标准。
  - 纯前端本地生成，不上传数据。
- **udi-decoder UDI码解码**：
  - 支持三种输入格式自动识别：① GS1 括号格式 `(01)…(17)…`（正则提取 AI+值）② GS1 Digital Link URI（`/01/val` 路径式与 `?01=val` 查询式两种）③ 行式 AI 键值（每行 `AI value`）。
  - 内置 GS1 AI 字典（约 30 条，医疗 UDI 常用：01/10/11/17/21/22/240/8018/8020 等）；日期型 AI(11/12/13/15/17) 按 GS1 世纪规则 YY≥50→19xx 还原 YYYY-MM-DD。
  - 输出字段表（AI / 含义 / 原始值 / 解析）+ 重构 GS1 字符串可复制。纯前端解析。
- WidgetHost.astro 注册 isUDIg / isUDId + 三档水合；两个 meta 描述/keywords 同步。
- 构建：85 → **88 页**（barcode 分类页 + 2 工具页），BUILD_EXIT=0。
- ⚠️ 修复：decoder FAQ 初版示例 URI 用了 `https://example.com/...`，致 dist 出现 example.com 泄漏（合规要求 0 泄漏）；已改 `acme.com` 并重建，全站 Grep 0 匹配确认。
- 预览 5189：udi-generator / udi-decoder / barcode 分类页均 200。工具总数 **30 个**。
- 注：生产发布仍待 ICP 备案通过；本次仅本地预览可见。

## 第十一批：AI 价格比价工具（旺财："AI 价格比价工具可以进行尝试，只对比主流大模型和plan+即梦等主流价格"）
- 新增 `ai-price-compare`（AI 价格比价）工具，category `ai`，icon 补 `coins`。
- 纯前端、零依赖、零后端、数据本地对比不上传。数据人工维护快照（标注「更新 2026-08，仅供参考」），非实时 API（符合之前 creaibo 分析结论）。
- 双 Tab：① **大模型 Token 价格**——18 条主流模型（OpenAI GPT-4o/4o-mini/o3/GPT-5.4、Anthropic Sonnet4.6/Opus4.8/Haiku4.5、Google Gemini3.5Flash/3.1Pro/3.1Flash-Lite、DeepSeek V4-Flash/V4-Pro、Qwen-Max/Plus、GLM-4.6、Kimi K2、豆包旗舰）；按厂商多选筛选 + 按输入/输出/名称排序；含 **场景成本估算器**（输入/输出 token → 各模型成本 USD+CNY 排序，从便宜到贵）。② **视觉生成价格**——即梦(详细：图片¥0.28/张、视频¥0.65-1.38/秒、会员档)、可灵/Vidu/Runway/Sora2/Veo3.1/海螺视频（标注「参考价」）。
- 价格来源：WebSearch 抓 2026-08 各厂商官方/评测价（OpenAI/Claude/Gemini/国产/即梦等），USD 为主，按 1 USD≈¥7.2 折算 CNY 参考（符合区域约定默认 ¥）。
- WidgetHost.astro 注册 isAPC + 三档水合。
- 构建：88 → **89 页**，BUILD_EXIT=0，Grep 0 example.com 泄漏。预览 5190：ai-price-compare + ai 分类页均 200。工具总数 **31 个**。
- 注：生产发布仍待 ICP 备案通过；本次仅本地预览可见。

## 用户当晚追加优先级（2026-08-04 22:52）
- 最急：**WorkBuddy 桌面端卡顿**（尤其「思考中」与「让我选择」弹窗）。诊断：官方文档(Overview)无性能/卡顿设置项；根因几乎确定是对话累积过长导致 webview DOM 变重。建议：大任务开新对话(可回读本工作记忆)、我侧回复写短、尽量少用「让我选择」弹窗。
- 待办队列（按用户排序）：① AI价格比价(本批已完成) ② 琥珀档真·AI对话舱——旺财要「参考链接看看」（下步给 OpenWebUI/LibreChat/Vercel AI SDK/Cloudflare Workers AI 等链接）③ 摩卡配色打磨 ④ 好站导航铺 100+ 网址 ⑤ SEO/投稿表单（稍后）。
- 备注：用户昵称旺财先生；AI 桌面搭子大美丽；每日祝「财运亨通，八方来财」。

## 23:17 决策：琥珀档降级 + 启动摩卡配色打磨
- **琥珀档 AI 对话舱 → 降为「待定」**：用户认可。理由——耗时大（最小自用版3-5天/像样版1-2周）、收益不确定（市面通用对话舱一堆、差异化弱）、合规红线（国内AI对话服务内容安全，个人小站不宜公开多用户）、多用户版超当前技术/资金能力。仅留后路：MokaKit正式上线后若有"工具箱就地问AI"场景，再做轻量自用版(Cloudflare Worker代理+自用Key，不做公开多用户)。
- **下一个项目 = 摩卡配色打磨**（好站导航100+其后，SEO/投稿表单稍后）。现状：自定义 daisyUI 双主题 toolbox/toolboxdark 写在 src/styles/global.css。诊断：主色偏暗黄棕彩度低、底色黄调重、暗色与摩卡调性脱节。打磨方向：更深可可主色+更中性燕麦底+拉对比+统一暗色 espresso。

## 23:18 进度工作台（随时查看进度）需求落地
- 旺财问"是否能做一个工作台随时查看进度"。结论：能，且用自动扫描实现，不需每次手动盘。
- 新增 3 个文件：
  - `workbench/status.json`：手动维护部分（部署/备案/里程碑/待办/批次日志）。
  - `scripts/build-workbench.mjs`：自动扫描 `src/tools/*/meta.ts` + `src/config/categories.ts` + `src/config/site.ts`，合并 status.json，生成自包含 HTML 看板（摩卡主题、可双击打开、不公开、零依赖）。
  - `workbench/workbench.html`：生成产物，看板含 KPI / 上线备案状态 / 里程碑时间线 / 分类分布 / 待办 / 全工具表 / 批次日志。
- package.json 加脚本 `"workbench": "node scripts/build-workbench.mjs"`。
- 首跑结果：工具总数 **31**、分类 9、空分类 3（计算/生活/趣味）。
- 数据策略：工具/分类等自动扫代码；备案/服务器/待办等手动维护在 status.json。可升级方向：① 接进每日 9 点自动化自动重生成并推送 ② 做成站内 /workbench 页（需考虑进度信息公开问题）。

## 23:19 摩卡配色打磨第一版落地
- 改 src/styles/global.css 双主题色板：亮色主色 oklch(45% 0.092 48) 可可棕（更深更饱和）、底 oklch(98.8% 0.005 60) 中性燕麦、accent 蜜桃粉保留；暗色 base oklch(22% 0.015 50) espresso、primary oklch(74% 0.092 55) 拿铁；BaseLayout theme-color meta 同步(#faf8f4/#211c18)。
- 构建 89 页过、无 example.com 泄漏，预览 5191 全 200。待旺财对比旧版反馈再微调。

## 23:48 页脚版本号 + 链接进度工作台
- 需求：© 2026 摩卡工具箱 MokaKit 后加版本号，版本号链接到进度工作台；正式版上线仅显示版本号不做链接。
- 实现：
  - `src/config/site.ts` 新增 `version: 'v0.9.0'` 与 `launched: false`（注释说明：正式上线改 true 即去掉链接）。
  - `src/components/Footer.astro` 版权行尾加版本号；`!SITE.launched` 时渲染为 `<a href="/workbench.html">`，`launched` 时渲染纯文本 `<span>`。
  - 进度工作台原在 `workbench/workbench.html`（不随站发布）。改 `scripts/build-workbench.mjs` 同时写 `public/workbench.html`，使 astro build 把它带进 dist，站点内 `/workbench.html` 可访问。
- 构建 89 页过；dist/workbench.html 生成；首页页脚渲染 `© 2026 摩卡工具箱 MokaKit <a href="/workbench.html">v0.9.0</a>`。预览 http://127.0.0.1:5192/ 全 200。
- 提醒：上线后 /workbench.html 仍可被直接 URL 访问（仅页脚不再链接）；如要完全隐藏，正式 --live 构建前删 public/workbench.html 即可。

## 23:59 复查 WorkBuddy「对话切换/新建卡顿」（续 22:52 桌面端卡顿）
- 用户报 website 项目「两个对话切换很卡 + 添加新对话也很卡」。先核实：**MokaKit 源码无任何对话/chat 功能**（全仓库 Grep conversation/chat/会话/聊天 0 命中，注册表无 AI 对话工具），故非项目代码 bug，而是 **WorkBuddy 客户端会话列表**性能问题（与 22:52 判断一致）。
- 实测数据：`~/.workbuddy/projects/d-WorkBuddy-website/` 共 3 个会话 jsonl，其中 `727e0067-2748-47cb-88ad-792311572909.jsonl` **16.1MB / 2770 条消息**（之前搭 MokaKit 的长会话）→ 切到它必须整文件解析，是该卡顿主因。`~/.workbuddy/logs/` 共 58MB（今天 22MB）可能加重整体迟缓。
- 已**安全备份**该大会话到 `C:\Users\zhao-\WorkBuddy-conversation-backup\727e0067-…-7909.jsonl`（原文件未动）。
- 根治：用户在 WorkBuddy UI 里删除/归档该 16MB 大会话（勿用 shell 删，避免 workbuddy.db 孤儿引用）；删后切换即顺。可选：清理旧 logs 减负。
- 标题：用户要求本对话标题定为「问题解决对话」——客户端会话标题需用户在 UI 点标题重命名，agent 无法改。
