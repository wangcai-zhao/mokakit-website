# 2026-08-25 工作日志

## 反向链接看板（SEO 入站流量）
- 背景：MokaKit 纯静态站，仓库内无任何入站反链数据（无 referrer 采集、`/go/` 不记日志）；公开搜索实测 `mokakit.com` 几乎零可检索反链（全是 mockkit/mochikit/mokkit 撞名站噪音）。
- 交付：`scripts/backlinks-report.mjs`（解析 GSC / 百度搜索资源平台导出 CSV，靠「mokakit 主机判定」区分外链域名与目标页，聚合 Top 域名/被链页/锚文本/平台归类）+ `seo/backlinks-report.html`（自包含暗色看板，已加 `noindex`）+ `seo/README.md`（导出指引）+ `seo/sample-*.csv`（示例）。`seo/` 已加进 `.gitignore`。
- 云同步：应旺财要求放到腾讯云 mokakit.com 主站 → `https://www.mokakit.com/backlinks.html`（ssh 管道直传 webroot）。验证 HTTP 200、noindex 生效。
- ⚠️ 重要坑：`deploy.sh` 是整目录替换 `/var/www/mokakit`（用 dist 重建），随手丢的 `backlinks.html` 下次 `deploy.sh` 会被清空。当前线上是**示例数据**（带「示例数据」横幅）。真反链需先从 GSC/百度导出 CSV 丢进 `seo/` 再生成。
- 待办（旺财确认后做）：把看板做成站内正式页面（集成进 Astro build 或 nginx 独立持久路径），避免被 deploy 冲掉、且用真实数据上线。

## 第十七批·数学计算器（10 个）+ SEO 博客
- 新增 10 个数学类计算器（calc 分类，全部无与现有重叠、高搜索量）：fraction-calculator 分数计算器 / average-calculator 平均数计算器 / ratio-calculator 比例计算器 / compound-interest 复利计算器 / gcd-lcm 最大公约数与最小公倍数 / roman-numeral 罗马数字转换 / rounding-calculator 四舍五入计算器 / scientific-notation 科学计数法转换 / permutation-combination 排列组合计算器 / discount-calculator 打折计算器。
- 每个均三件套：meta.ts（自动注册）+ Tool.tsx（交互逻辑，含 copy 助手 + daisyUI 样式）+ content.mdx（六段式 SEO 文：原理/能做什么/怎么用/详解/算例/FAQ）。WidgetHost.astro 手工接线（import + isXxx + known + 3 行渲染，集中 4 处编辑）。
- 另写 1 篇 SEO 深度指南 `src/content/blog/math-calculators-guide.md`（10 工具全内链，glob 自动收录进博客）。
- 构建踩坑：discount-calculator 初版 `useMemo(calcItem, [a])` 误把 calcItem 当无参调用 → SSR 报「Cannot read properties of undefined (reading 'price')」；改为 `useMemo(() => calcItem(a), [a])` 修复。其余 9 个 useMemo 本来就是箭头函数包，无此问题。
- 构建环境反复卡死（6+ 分钟零输出）：清 .astro / node_modules/.vite / dist 仍卡；查到系统残留 3 个 13:xx 启动的孤儿 `npm run build` 进程（挂 1.5 小时）抢资源，kill -9 后重跑通过（日志 `✓ Completed`）。
- 验证：dist 总 HTML 434、工具目录 116、10 新工具页齐；`gen-sitemap.mjs` 重生成 sitemap → 429 条 URL（含 10 新工具）。
- 部署：已校验 dist 直接 tar 上传 + nginx reload（**未重建**，避开构建卡死），原子替换旧版留 /var/www/mokakit.old。线上核验 /tools/fraction-calculator/、/tools/compound-interest/、/tools/discount-calculator/、/tools/permutation-combination/、/blog/math-calculators-guide/、/sitemap-index.xml 均 HTTP 200；首页已列出新工具。
- ⚠️ git PAT 仍失效，本次源码未提交 GitHub；建议旺财恢复 PAT 后 `git push`（远端 master）入库，否则本地与仓库漂移。

## 第十八批·实用计算器（10 个）+ SEO 博客
- 新增 10 个高搜索量实用计算器，**避开与现有重叠**（先 Glob 排查确认贷款/房贷/BMI/百分比/小费/年龄/日期差/折扣/个税社保年终奖等均已存在）：
  - calc 7：provident-fund-calc 公积金（基数×(个人+单位比例)）/ inflation-calculator 通胀（M×(1+r)^n 双向）/ log-calculator 对数（换底公式，附 ln/lg）/ power-root 幂与根（a^b + ⁿ√a，tab 切换）/ prime-factor 质因数分解+素性判定（≤1e12）/ hourly-wage 时薪↔月薪（默认 21.75 计薪天）/ gpa-calculator 多课程动态加行（4.0/5.0 制加权）。
  - life 3：bmr-calculator 基础代谢率（Mifflin-St Jeor）/ menstrual-cycle 月经周期（周期−14=排卵日，易孕窗口±2天）/ shoe-size 鞋码（中/欧/美女/美男/英码以脚长为中枢互转）。
- 每个均三件套：meta.ts（声明式+SEO，hydrate:'load'，priority:7）+ Tool.tsx（交互逻辑 + copy 助手 + daisyUI）+ content.mdx（六段式）。WidgetHost.astro 手工接线（import + isXxx + known + 3 行渲染，4 处编辑，30 行）。
- 修正：bmr-calculator/meta.ts 的 related 去掉不存在的 `calorie-calc`，改为 `['bmi-calculator', 'menstrual-cycle', 'after-tax-salary']`；gpa/content.mdx 的 `<60→0` 改全角 `＜60→0`（MDX 裸 `<` 当 JSX 报错）。
- 另写 1 篇 SEO 深度指南 `src/content/blog/everyday-calculators-guide.md`（title「10 个实用计算器盘点：公积金、时薪、GPA、基础代谢率一键算（附在线工具）」，10 工具全内链 + 真实算例，glob 自动收录进博客）。
- 构建踩坑：① 首次构建卡死（6m41s 零输出，vite 重优化死锁）→ TaskStop 后改 `astro build --force` 全量重建通过（466 页）；② gpa content.mdx 第 20 行 `Unexpected character after '<'` → 全角修复后普通 build 通过（日志 `_b18c.log` 末行 `✓ Completed in 1.88s`）。
- 部署：已校验 dist 直接 tar 原子上传 + nginx reload（**未重建**，避开卡死）。⚠️ 首传发现 sitemap-index.xml 404 —— 本次只跑了 `astro build` 漏掉 `gen-sitemap`，补跑 `node scripts/gen-sitemap.mjs`（440 条 URL）后单独补传两个 sitemap 文件修复。
- 线上核验（服务器侧 curl --resolve）：10 工具页 + /blog/everyday-calculators-guide/ + /sitemap-index.xml + / 全部 200；10 新工具 URL + 博客 URL 均已进 sitemap-0.xml；首页已挂载新工具链接。
- 工具总数 104→**123**（calc 54 / dev 23 / text 8 / security 6 / ai 6 / convert 6 / life 8 / fun 5 / clock 4 / barcode 3）。
- ⚠️ git PAT 仍失效，第十八批源码未提交 GitHub；待恢复后 push master 入库。

## 变现落地 + 复刻 1 工具（晚间）
- **ads.enabled=true**：site.ts 广告开关打开（变现落地第一步）。⚠️ 但 `adsenseClient` 仍为空串，AdSlot.astro 门控 `enabled && adsenseClient` 为 false → 广告暂不渲染；需旺财提供真实 `ca-pub-XXXX` 填入才真正出广告。已同步更新注释。
- **新增 combo-loan-calc 组合贷月供计算器**（calc 分类，中国购房者刚需、与 loan-calculator 单贷明确区分、金融类利于变现）：三件套 = meta.ts（icon 用已存在的 `landmark`）+ Tool.tsx（公积金/商贷两笔等额本息分别核算后求和，含复制助手 + daisyUI）+ content.mdx（六段式：公式/能做什么/怎么用/为什么分开算/算例/FAQ）。WidgetHost.astro 手工接线 4 处（import + `const isComboLoan` + known 链 + 3 行渲染块）已校验一致。
- **mokakit.cn 备案落地线上核验（通过）**：`curl --resolve` 验证 `https://mokakit.cn/` 与 `https://www.mokakit.cn/` 均返回 301 → `https://www.mokakit.com/`（HTTP/2，证书有效），整站跳转规范域干净。备案 京ICP备2026051111号（同主体）已接入完成。
- 全站 `astro build` 后台验证中（task qdnAzk）；完成后需补跑 `node scripts/gen-sitemap.mjs` 把新工具写进 sitemap，再 tar 上传 + nginx reload 部署（git PAT 仍失效，无法直接 push master，需旺财恢复）。
- MEMORY.md 已做去重精简（留存关键环境坑与战略主线）。
