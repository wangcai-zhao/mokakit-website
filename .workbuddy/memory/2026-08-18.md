# 2026-08-18 工作日志

## 本地 vs 生产环境差异核对
- 生产部署时间戳：/var/www/mokakit/index.html = 2026-08-15 04:07（deploy.sh 直接 build 工作树发布，非按 git commit）。
- **一致项**：百度统计 ID 均为 c1ce047dfb4bbcfe58cba2418b56332a；clock 分类 + 4 个时钟工具（analog/digital/flip/world）已上线；MCP 100 tools/v1 服务 active；nginx 含 .cn + ssl include 正常；ICP/公安备案号一致。
- **本地领先未部署**：`/time/` 时区工具栏目（src/pages/time，全新，生产无 /time）；ToolLayout(-149)、index.astro(+68)、BaseLayout/Header/WidgetHost/tools/[tool]/c/[category] 等模板优化未提交未上线；未跟踪资源 generated-images/、public/clock-bg/、public/baidu_verify_codeva-*.html、ClockImmersiveLayout、clock 共享组件等。
- **双方均缺失（隐患）**：`sitemap-index.xml` 在生产与本地 dist 都不存在 → gen-sitemap 未跑/产物未进 dist，SEO 硬伤，亦阻塞「提交 sitemap 到站长平台」待办。
- **认知陷阱**：git 工作树脏 ≠ 未部署（deploy 直接 build 工作树）。判断差异应以"当前 src 工作树 vs 生产构建"为准。
- **结论**：生产比本地 dist 文件夹更全（本地 dist 仅 2 个 clock 工具、过期），但 deploy 会重建，无回退风险。建议：先 `node scripts/gen-sitemap.mjs` 补 sitemap，再提交 /time + 模板改动后 `bash deploy/deploy.sh root@58.87.68.151` 一次性上线。
