# 2026-09-03 工作日志

## 天气查询工具（Open-Meteo）闭环
- 用户想接 Azure Maps 天气 API（文档：learn.microsoft.com/.../how-to-request-weather-data），但该源需付费 subscription-key + 无前端 CORS，纯静态站不可行。
- 改采 Open-Meteo（免费/免注册/免key/CORS友好），纯前端 fetch 地理编码+实时天气+7天预报。WMO code 中文映射。
- 新增 src/tools/weather/{meta.ts(life分类,icon cloud-sun), Tool.tsx, content.mdx(六段式)}；WidgetHost.astro 4处接线完成。
- 本地 astro build 绿（BUILD_EXIT=0），天气页水合、旧20工具不受影响、cloud-sun 图标已打包。
- 部署生产：SSH 前台 + dangerouslyDisableSandbox:true + 显式 -i ~/.ssh/mokakit_deploy -o IdentitiesOnly=yes，原子切换成功。prod tools 145→146 页，https://www.mokakit.com/tools/weather/ 返回 200。
- 本地 git commit 039c946（4文件 +280行）。**GitHub 推送失败**：PAT ghp_EI1HEpTccgDXFgSLTcTJ 已失效（用户按 09-02 建议 revoke），需新 PAT 或本机 GCM 直推。
- ⚠️ 复用经验：run_in_background 组合下 dangerouslyDisableSandbox 不生效（私钥读取被沙箱拦→permission denied），SSH 上传必须前台 + 沙箱绕过。

## 待办
- GitHub 远端同步天气工具 commit（本 Agent 环境推不上去，需用户本机 push）。

## 本环境推 GitHub 的两条死路(已验证, 别再试)
- git over HTTPS: 环境注入 HTTPS_PROXY=127.0.0.1:64911(WorkBuddy 服务代理), 对 github.com:443 返回 502; 直连 github.com:443 也超时(出站封禁)。即 git 传输协议在本 Agent 环境完全不可达。
- GitHub MCP 连接器(mcp__github): 只读权限, push_files 报 403 Resource not accessible by integration, 无写权限。
- ✅ 可行: 用户本机终端 `git push`(GCM OAuth, 网络可达有权限)。或用户重发 PAT 后本机推。
- 天气 commit(039c946)已安全落本地, 生产已上线(146页), 仅 GitHub 远端待用户本机同步。

## sitemap
- sitemap 已随 build 更新; 可提交百度/Google 站长平台。

## 「单位换算扩军」一波 20 个工具（闭环）
- 方向：单位换算扩军（刻意避开 unit-convert 已覆盖的 16 类：length/weight/temperature/area/volume/time/speed/data/data-rate/power/energy/pressure/angle/force/torque/frequency），数量 20 个，全部 category=convert，配套六段式 content.mdx（为什么→概念/公式→细分对照表→怎么用→场景实例→隐私）。
- 抽离通用线性换算组件 src/tools/_shared/linear-convert.tsx（LinUnit 接口 + 以基准单位×factor 纯比例中转），14 个标准比例类 Tool 仅传单位表，质量更稳、代码更短。
- 14 比例类：resistance / capacitance / inductance / voltage / current / data-binary / flow-rate / illuminance / typography / density / viscosity / angular-velocity / acceleration / magnetic。
- 1 非线性：fuel-efficiency（以 L/100km 为基准中转，km/L 与 mpg 互为倒数，含零值保护「能效类数值不能为零」）。
- 5 查表类：ring-size（内周长mm→美/英/港/欧/日/中）、hat-size（头围cm→美/英/欧/中）、bra-size（下围cm+罩杯→欧/美英/法/日）、paper-size（A0–A8/B4–B6/C4–C6/Letter/Legal/Tabloid 规格表 mm/cm/in）、baking（杯勺体积换算 + 10 种食材 1 杯约克重表）。
- 三件套 meta.ts + Tool.tsx + content.mdx 全建；icon 用标准 lucide 名（zap / battery-charging / waves / plug / fuel / memory-stick / droplet / lightbulb / type / layers / droplets / rotate-cw / rocket / magnet / gem / printer / tag / shirt / utensils）。
- WidgetHost.astro 4 处/工具接线（import + isXxx 布尔 + known 链 + client:load 渲染块），20 个全接，**零死工具**（dist 抽样含岛屿静态渲染）。
- 构建：noop-shim + astro build 绿（BUILD_EXIT=0，490 页）。⚠️ 教训：`| tail -40` 管道会缓冲输出、易误判「收尾挂死」空跑 11 分钟；改用 `> _build_new.log` 写日志则 39s 完成。下次构建直接写日志文件。
- 待部署 + 本地 git commit；GitHub 推送交用户本机（沿用天气工具结论：Agent 环境 git 出站不可达，HTTPS_PROXY→github 502，GitHub MCP 仅只读）。
- 站点规模：工具 146（天气已上线）→ 166（本次 +20），dist 490 页。

## 待办
- 部署生产（SSH mokakit_deploy 上传 dist + 原子切换 + nginx reload）。
- 本地 git commit 本次 20 工具三件套 + WidgetHost 接线 + 共享组件；GitHub 远端同步交用户本机 push。
