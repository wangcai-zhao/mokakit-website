# 2026-08-16

## 首页新增「今日推荐」数字时钟
- 任务：在摩卡工具箱首页添加数字时钟的链接与推荐。
- 改动：`src/pages/index.astro`
  - frontmatter 引入 `getTool`，取 `digital-clock` 的 meta（动态同步，不硬编码）。
  - 在 hero 区之后、分类网格之前插入「今日推荐」卡片：标题 + tagline + 简介，整体为链接指向 `/tools/digital-clock/`，右侧带实时时钟预览（`#home-rec-clock`/`#home-rec-date`，1s 刷新）与「打开」按钮。
- 验证：已有 dev server（:4324）热更新后 `curl` 首页 HTTP 200，含「今日推荐」「/tools/digital-clock/」「home-rec-clock」「数字时钟」全部命中。
- 备注：生产 `astro build` 在本沙箱仍卡在 "Collecting build info" 后（vite 依赖重优化偶发卡死，疑似与已运行的 dev server 共用 .vite 缓存锁冲突）。代码本身已验证可编译渲染，部署走 `npm run build` + `deploy/deploy.sh` 即可（建议在干净环境/部署机执行）。

## 首页导航新增「时间日期」聚合入口
- 任务：把时间、日期相关工具做成一个聚合页，并在首页导航栏加链接。
- 新增 `src/data/timeDate.ts`：6 个分组的 curated 配置（时钟 / 日期计算 / 时区换算 / 计时·倒数 / 时间戳&表达式 / 今天），列出各工具 id。
- 新增 `src/pages/time/index.astro`：时间日期聚合专题页，hero 实时时钟+日期，按分组渲染 ToolCard，含 breadcrumb + JSON-LD。sitemap 扫 dist 自动收录 `/time/`，无需额外配置。
- 改动 `src/components/Header.astro`：桌面主导航与移动端菜单各加 `时间日期` → `/time/` 链接（含 active 态 `pathname.startsWith('/time')`）。
- 验证：dev server 热更新后 `/time/` HTTP 200，6 个分组标题与工具卡齐全；首页 nav 含 `<a href="/time/">时间日期</a>`。
