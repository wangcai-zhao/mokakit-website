# 2026-08-12 工作日志

## 六段式小标题归一化 + 重建
- 继续六段式补全的收尾：核验 dist 实际覆盖率，发现 94 个工具页里只有 63 个用标准小标题「这个工具能做什么」，另有 31 个用了「什么是X」等变体。
- 拆解 31 个：20 个「生成批」（calc/换算类，标准六段式只第2段措辞不同）→ 全部归一化为「这个工具能做什么」（`scripts/normalize_h2.py` 批量替换首个 `## 什么是` 行）；1 个 ode-solver 本就合规保留；10 个手写批（password-generator/build-your-own-x/public-apis/system-design/github-stars/dev-roadmap/commit-gen/changelog-gen/license-gen/gitignore-gen）为刻意编辑/指南式结构，硬套会伤内容，保留。
- 重建踩坑：原 `NODE_OPTIONS="--require=noop-shim.cjs --use-system-ca --max-old-space-size=4096"` 在当前内存吃紧环境**静默 OOM 连锅端**（卡在 Collecting build info 后 113ms Exit 1 / 或日志全空）。改为只留 `--require=noop-shim.cjs` 即稳定过（构建约 4s，收尾仍可能因 vite 句柄报 Exit 1 但 dist 已写全）。已把此坑写入 MEMORY.md「环境与坑」。
- 验证：dist 工具页 94、全站 html 395、「这个工具能做什么」页 63→83、源码 0 残留「## 什么是」。
- 该坑同步更新到项目 MEMORY.md 的构建命令说明。

## 当前状态
- 六段式深度内容：94/94 = 100% 收口（含归一化）。
- 下一步战略方向（未执行）：MCP Server（战略核心，本地 compute 层已就绪可先跑通）/ 分享+计数+统计（基建就位仅开门禁）/ git 版本管理（当前无仓库）/ 等 ICP 备案后生产上线。

## 首页 / 好站导航轻量焕新（与 MCP 并行，已落地）
- 旺财反馈首页偏单调，评估「好站导航加图片」→ 可行、零耦合（sites.ts 不在 MCP 链路）。
- 新增 `src/utils/favicon.ts`：`faviconOf(url)` 从 `link.url` 派生 `https://{hostname}/favicon.ico`，零第三方依赖、try/catch 容错。
- 好站链接卡（`src/pages/sites/[category]/index.astro`）：字母头像改为「底层字母 + 上层 favicon `<img>`（`loading=lazy`/`referrerpolicy=no-referrer`/`onerror` 隐藏露字母兜底）」。**494 条零数据录入**。分组卡（`/sites/index.astro`，Lucide 图标）保持不动。
- 首页（`src/pages/index.astro`）：新增「精选好站」带图区块（跨前 4 组各取 2 条、上限 8、链 `/sites/<group>/` 内页）；hero 加 `from-primary/5 to-base-100` 主题感知渐变。
- 重建验证：全站 html 395 不变；ai-chat 页 37 个 favicon、首页 8 个精选卡均生效。扰不动 `src/lib/*.ts`/`meta.ts`/`registry.ts` → 对后期 MCP 零影响。
- 约定：MCP 与版面并行推进；MCP 三原则（同源发现 / compute 入参即契约 / 描述从 meta 生成）已写入方案 `plans/blazing-vortex-darwin.md`。

## 备案状态更新（旺财 19:35 告知）
- mokakit.com **ICP 已批复：京ICP备2026051111号**（工信部号）。
- **公安联网备案进行中**（未批）。
- mokakit.cn 备案**已提交、等待结果**。
- 备案号落点已确认：全站单一真相源 = `src/config/site.ts` 的 `icp`（ICP 号）/`police`（公安号）字段（现空），页脚 `Footer.astro:80-91` 在字段非空时自动渲染并链 `beian.miit.gov.cn`。上号即全站显示，零额外改动。
- 对上线的影响：.com 号已齐，**仅剩公安备案 + .cn 批复**两道外部关卡；旺财拍板上线后 → 填 site.ts → A 记录 → `deploy.sh --live` → `server-setup.sh --cert` → `--enable-ssl`。
- 已同步更新 MEMORY.md 的「项目身份」域名行与「待办队列 #1 备案」项。

## 上线配置落地（旺财 19:38 拍板：填号 + 上线，.cn 暂藏）
- 改 `src/config/site.ts`：`icp='京ICP备2026051111号'`、`launched:true`、`altDomains:[]`（移出 mokakit.cn）、`police:''`（公安号待批故留空）。公安备案不阻塞上线。
- 重建踩坑：首跑在「Collecting build info」后 150ms 被掐（dist 0 html），是瞬时内存压力 OOM 连锅端；重试即过（`FINAL_EXIT=0`、`394 page(s) built`、`Complete!`）。结论：该 OOM 是**概率性瞬时**的，重试可解，必要时清 .astro 缓存 + 杀残留进程再战。
- 验证：页脚显示 京ICP备2026051111号 + 链 beian.miit.gov.cn；launched 生效后页脚不再链 /workbench.html；dist 里 mokakit.cn 出现 0 次（出站白名单已清）；全站 html 仍 395。
- 注意：这只是**源码上线配置**就绪并本地构建验证。真正推到生产服务器还需：绑 deploy 公钥 → A 记录 → deploy.sh --live → server-setup.sh --cert → --enable-ssl。等旺财拍板走基建。

## MCP Server 本地版 v0.1.0 跑通（按计划执行）
- 新建 `mcp/meta-loader.mjs`（纯 JS，扫描 `src/tools/*/meta.ts` 解析出 94 工具目录，new Function + 正则兜底，0 解析错误）+ `mcp/server.mjs`（零外部依赖 Node http/crypto，Streamable HTTP + JSON-RPC 2.0）。
- 三原则落地：① 同源发现（复用 meta.ts，不写第二份清单）② compute 入参即契约（直接 import `src/lib/{china-tax,china-social-security,china-vat,mortgage}.ts` 纯函数，仅依赖 decimal.js）③ 描述从 meta 生成（MCP tool 描述取自对应 meta.ts）。
- 9 个 tool：8 计算型（个税/年终奖/年终奖对比/五险一金/增值税一般/增值税简易/房贷计划/房贷提前还款）+ `mokakit_search`（search-first 在 94 工具目录检索，命中计算型工具附带 mcpTool 名）。
- package.json 加脚本 `"mcp": "node --experimental-strip-types --no-warnings mcp/server.mjs"`。
- 验证（curl 走完整 JSON-RPC）：initialize 握手成功（session + 协议 2025-06-18）→ tools/list 返回 9 → tools/call(income_tax_cn) 数值与手算一致（应税17.6万→税18280）→ mortgage_early_repayment_cn / mokakit_search 均正确。
- 踩坑：块注释里写 `src/tools/*/meta.ts` 含 `*/` 会提前闭合注释报 SyntaxError，改成 `各工具目录下的 meta.ts` 解决。
- 服务本地常驻在 http://localhost:18700/mcp（后台进程 YLE0xL）。公网 HTTPS 接入待备案后，可复用 counter 的 systemd/nginx 班车。已写入 MEMORY.md「MCP Server」专节。

## 分享按钮 + 点击计数 + 访问统计 上线（Quick Win 收口）
- 探查：分享按钮前端 JS 早已完整（ToolLayout 的 #tool-share-btn 绑 navigator.share+复制兜底，SITE.share.enabled 早已 true）；计数链路（#tool-use-count / #site-visits + src/utils/counter.ts）也已写好，唯独 SITE.counter.enabled 仍是 false。
- 改 src/config/site.ts：counter.enabled: false → true（一行开门禁）。计数服务端 deploy/counter/server.mjs（零依赖 Node，127.0.0.1:18800，落盘 /var/lib/mokakit/）+ systemd + nginx /api/ 反代基建早已就位。
- 重建（只留 shim）首跑又遇瞬时 OOM 把 dist 清空；重试 FINAL_EXIT=0、394 页、Complete!。
- 本地完整闭环验证：起 counter server（DATA_DIR 设项目内 .counter-data 避 Windows 路径怪异）+ 新写 scripts/preview-counter.mjs（纯 Node：服务 dist 静态 + 反代 /api/* → 18800）。经 60991 端口 curl 验证：GET/POST /api/count 正确转发累加、首页 200、工具页含 #tool-use-count、落盘 counters.json 生成。preview 面板开 60991 即可真跑通计数 JS（数字填充）。
- 注：60990 被上轮遗留 python http.server 占用（无反代），改用 60991 起 node proxy；旧 python 未清但不影响验证。
- 收尾：workbench status.json 该待办置 done:true，重跑 npm run workbench 同步 dist/workbench.html。MEMORY.md 待办 #2 改 ✅。
