# 2026-08-09 工作日志

## 新增工具：微分方程求解器 ode-solver
- 来源：用户要求收编 https://www.bchrt.com/tools/differential-equation-solver/。该页真实可用功能仅「一阶 ODE 初值问题 + RK4 数值解」，符号解/PDE 只是 SEO 文案 → 只实现一阶 ODE。
- 新增文件 `src/tools/ode-solver/`：`solve.ts`（纯 TS 计算层：手写递归下降表达式解析器 + RK4 积分，零依赖）、`examples.ts`（6 个经典方程，喂 UI 一键填充 + 长尾子页）、`meta.ts`、`Tool.tsx`（实时求解 + 手写 SVG 解曲线 + 采样表 + JSON 导出 + 示例库）、`content.mdx`（六段式长文）。
- 接线 2 处：`WidgetHost.astro`（import / isODE 布尔 / known 链 / 三档渲染 共 4 处）；`[...sub].astro`（ode-solver 分支：传 initial 参数给岛屿 + 构建期算 RK4 采样表写进 HTML 供爬虫）。
- 图标用现成 `activity`；不改 `package.json`（零依赖）。
- 验证：node 断言 6 示例正确（`-2*y+x` → y(2)=0.772895 对齐原站 0.77289463；`x*y`→54.597839≈e⁴）。`npm run build` 成功，364 页（357+主工具页1+子页6），sitemap 362 条。主工具页非「开发中」兜底、默认解已 SSR 进 HTML；子页含真实 20 行采样表。
- 踩坑：`node --experimental-strip-types` 纯擦除模式不支持 parameter properties，把 `Parser` 的 `constructor(private tk)` 改成普通字段赋值（Vite/esbuild 不受影响，但这样能本地直跑验证）。
- 进度看板：`npm run workbench` 已刷新（工具数 68→69），`cp public/workbench.html dist/workbench.html` 已做。
- 预览：`npm run preview` 在 http://localhost:4327/（4321-4326 被占，顺延 4327）。工具页 /tools/ode-solver/，子页 /tools/ode-solver/<linear-decay|logistic-growth|forced-vibration|parabolic|exp-driven|product>/。
