# 2026-09-17 工作日志

## 顶部导航换行治理 + 页脚版本/社交入口（未部署，仅本地 dist）
- **起因**：主导航标签过多（7 个文字入口 + 分类）在中屏会挤到折行。
- **做法**：
  - `src/components/Header.astro`：桌面主导航只留 全部工具 / 好站导航 / 智能体（+ lg 时间日期 + xl 三个分类）；博客 / 更新日志 / 技巧 / 开发者 / 关于 收进右上角「更多」`<details>` 下拉（MORE_LINKS 常量驱动，当前页高亮）；移动端菜单底部同组一组 MORE_LINKS。
  - 新建 `src/components/SocialLinks.astro`：GitHub + mailto 内联 SVG（Lucide 风格），variant=square/inline，Header 右侧（hidden sm:inline-flex）、移动端菜单（sm:hidden）、页脚共用。
  - `icons.ts` 补 `chevronDown`；Header 内联 script 补「点击外部 / Esc / 点链接后收起下拉」。
  - 版本号单点化：`src/config/site.ts` 改为 `version: \`v${pkg.version}\``（import '../../package.json'），package.json `0.1.0 → 0.9.11`；`scripts/build-workbench.mjs` 改从 package.json 读版本（原正则抠 site.ts 已失效）；workbench-data.json siteVersion 同步。
  - 新增 `SITE.email` + 派生 `SOCIAL`（github/mailto/email）。
  - Footer：品牌区加社交图标按钮；底部链接补「博客」。
- **校验**：build 通过 535 页 + sitemap 530 URL；脚本核对主导航 7 项、下拉 5 入口、页脚 `© 2026 摩卡工具箱 MokaKit v0.9.11`、GitHub/mailto 各 2 处；14 个导航目标页全部存在 index.html。
- **坑复现**：首次 build 卡在 "Collecting build info" 后 14 分钟无输出 → 清 `.astro`（mv 到 `_astro_bak_navfix_0216`）后 12.48s 完成，印证「缓存损坏」结论。node 实际路径是 `versions/22.22.2-3/node.exe`（不是 22.22.2）。
- 本地预览：`python -m http.server 4399 --directory dist`（127.0.0.1:4399）。**未 git commit、未上生产**。
