# 2026-08-13 工作日志

## 生产推送收口（ICP 备案后全链路上线）
- 完成「绑 deploy 公钥 → A 记录（.com + www）→ `deploy.sh --live` 推真实站点 → `server-setup.sh --cert`（certbot webroot 签发）→ `--enable-ssl`」。公网 HTTPS 正式上线。
- 实测公网：
  - `http://mokakit.com` 301 → `https://mokakit.com/`（单跳）
  - `http://www.mokakit.com` 301 → `https://mokakit.com/`（单跳，无 http→https 双重跳）
  - `https://www.mokakit.com` 301 → `https://mokakit.com/`
  - `https://mokakit.com` 200 + HSTS `max-age=31536000`
  - 证书 Let's Encrypt YE2，2026-08-12 ~ 2026-11-10；certbot.timer 已激活自动续期；续期钩子 reload nginx。
  - 页脚 MokaKit / 摩卡工具箱 / 京ICP备2026051111号 齐全；`/api/health` 正常；`/robots.txt`、`/sitemap-index.xml` 200。

## 修复的惨痛坑（已固化进 MEMORY.md）
- **nginx conf.d 双重加载**：原设计把 `mokakit-ssl.conf` 放进 `/etc/nginx/conf.d/` 且 `mokakit.conf` 里又 `include` 它 → 443 块被加载两次 → `conflicting server name ... ignored` 把 www 跳转块吞掉 + `protocol options redefined` 告警。
  - 修复：ssl 配置改放 `/etc/nginx/mokakit-ssl.conf`（conf.d 之外），`mokakit.conf` 末尾 `include /etc/nginx/mokakit-ssl.conf;`。`--enable-ssl` 的 sed 解注 include 时同步改路径（兼容注释/未注释、新旧路径）；`--live` 清理两处残留。
- **Let's Encrypt YE2 证书无 OCSP URL** → `ssl_stapling off;`（否则 `ssl_stapling ignored` 告警）。
- **nginx 1.24.0** 用 `listen 443 ssl http2;`（非 `http2 on;`），两 443 块都加 http2 消除 redefined 告警。
- **acme.sh 装不了**：服务器出网 github.com/raw.githubusercontent.com 被墙；改用 `apt-get install certbot` + `certbot certonly --webroot`。

## 修改的本地文件
- `deploy/nginx/mokakit-ssl.conf`：www 块加 http2；stapling 改 off。
- `deploy/nginx/mokakit.conf`：include 路径改为 `/etc/nginx/mokakit-ssl.conf`（注释态，作为唯一开关）。
- `deploy/server-setup.sh`：`enable_ssl()` 的 SSLCONF 改 `/etc/nginx/mokakit-ssl.conf`、清理 conf.d 副本、include 解注 sed 兼容新旧路径；`--live` 清理两处残留。
- 临时修复脚本 `scripts/_fix-ssl.sh`（一次性恢复用，含 /tmp 硬编码，可删）。

## 待跟进
- mokakit.cn 备案过审后加 DNS + 改 `site.ts` 的 `altDomains`。
- MCP Server 公网 HTTPS 接入待排期。

## 公安联网备案号上线（同日追加）
- 批号 `京公网安备11010502062390号`。改 `src/config/site.ts` 的 `police` 字段填号；`src/components/Footer.astro` 把公安备案号从纯 `<span>` 升级为可点击核验链接（从 `police` 提取数字拼 `https://beian.gov.cn/portal/registerSystemInfo?recordcode=11010502062390`，与 ICP 同款 `rel="nofollow noopener"`；政府备案链接不走 `/go/`）。
- 重建：`export NODE_OPTIONS="--require=D:/WorkBuddy/website/noop-shim.cjs"` 后 `node node_modules/astro/bin/astro.mjs build && node scripts/gen-sitemap.mjs`（395 页 + sitemap 393 条）。
- 推送：tar dist → `ssh root@58.87.68.151` 原子替换 `/var/www/mokakit`（上一版留 `.old`）+ chown www-data + reload nginx。
- 验证（本机沙箱出站 HTTPS 被拦，改走服务器自测）：`ssh ... 'curl -s --resolve mokakit.com:443:127.0.0.1 https://mokakit.com/'` 确认首页/分类页均含公安号 + 核验链接 + HSTS 头。裸 `127.0.0.1` 会命中兜底 `return 444`，须带 `--resolve`。

## 项目全量档案存档（上线日快照）
- 旺财要求分析整理这段时间项目全部信息 + 进度过程并存档，为「新运营」启动铺垫。
- 产出：`archive/MOKAKIT_PROJECT_ARCHIVE_2026-08-13.md`（立项 08-04 → 上线 08-13 全量信息 + 进度时间线 + 环境坑 + 新运营分层建议）。配合 MEMORY.md 使用。
- 数据已从 9 天日级日志 + git + 实际构建实测提炼；关键口径差异（工具数 31→67、好站导航 31组494条 vs 08-11 记 36组624条）已在档案标注，以最终 94 工具 / 上线日实测为准。
- MEMORY.md 会话管理段补「上线日全量档案」入口指针。
- ⚠️ 待跟进项写入档案 §7 #7：08-13 上线日改动（deploy/nginx/*、deploy/server-setup.sh、deploy/deploy.sh、Footer.astro、site.ts、MEMORY.md、scripts/_fix-ssl.sh）尚未 commit，待旺财确认后 `git add -A && git commit`。

## 上线改动已 commit（旺财授权「开始干」）
- commit `86ae091`「chore(release): 公网上线收口」。精确 add 上线相关 9 文件：**排除**运行日志 `build-run.log`(已跟踪)/`deploy-run.log`(未跟踪) 与一次性临时脚本 `scripts/_fix-ssl.sh`(含 /tmp 硬编码)。
- 已提交：deploy/deploy.sh、deploy/nginx/mokakit-ssl.conf、deploy/nginx/mokakit.conf、deploy/server-setup.sh、src/components/Footer.astro、src/config/site.ts、.workbuddy/memory/MEMORY.md、2026-08-13.md、archive/MOKAKIT_PROJECT_ARCHIVE_2026-08-13.md。
- 版本库现状：master 三提交 `86ae091`→`7bd2dcb`→`2eabc3d`，真实反映公网状态。
- 遗留未跟踪/未提交：build-run.log、deploy-run.log、scripts/_fix-ssl.sh（噪音/临时，建议后续清理或加 gitignore）。

## 百度统计激活（已上线）
- 接口已确认：`src/config/site.ts:54-59` 的 `analytics.baiduId` 当前空串；`src/layouts/BaseLayout.astro:146` 仅在 ID 非空时注入 `hm.baidu.com/hm.js?<id>`。激活 = 填真实 ID + 重建 + 推生产。
- 旺财提供真实 ID `944d68a7dfe7ae7fab7d0db7d5d6fa12`，填入 `site.ts` 的 `analytics.baiduId`（define:vars 注入、URL 用 `+id` 拼接，故正文无字面 `hm.js?<id>`）。
- 重建（astro build 偶发 Exit 1 假错 → 重跑一次拿完整 dist）+ sitemap（393 条）→ 等效 `deploy.sh --live` 的 upload + live 切换两段推生产。
- **踩坑**：重跑 `--live` 触发 `server-setup.sh` [6/8] 的 `rm -f mokakit-ssl.conf`，443 整段丢失、HTTPS 全灭；补跑 `server-setup.sh --enable-ssl` 恢复。已写入 MEMORY.md 环境坑（下次重跑 --live 必补 enable-ssl）。
- 服务器自测全绿：443 监听 / 首页 HTTPS 200 / 百度统计已注入 / HSTS / http→https 301。commit `12a91b7`「feat(analytics): 启用百度统计」。
- 运营起始态：百度统计已看数；下一步盯 certbot 续期（11-10 到期自动续）、SEO 抓取验证、MCP 公网化排期。

## SEO 抓取验证（已做 + 修复）
- 服务器自测（ssh 当客户端、`--resolve` 命中正式 server 块）全量抓取信号：
  - **达标**：robots.txt 可访问 + Sitemap 指令；sitemap-index.xml 200 + 392 条 URL；首页 / 9 分类页(`/tools/c/<slug>/`) / 工具总览(`/tools/`) / 好站导航(`/sites/` + 36 分类) / about / privacy / 长尾子页 全部 200 + title/description/canonical/robots(index,follow)/og/twitter/JSON-LD 齐全；`/go/` 中转页 noindex；404 页返回 404。canonical 均为绝对 https URL，无 noindex 误杀。
  - 分类页路由是 `/tools/c/<slug>/`（convert/text/security/calc/dev/barcode/ai/life/fun），非 `/tools/<slug>/`。
- 修复两处自洽瑕疵（commit `bc41b91`）：① `public/robots.txt` 与动态端点 `robots.txt.ts` 冲突，统一为完整版（补 `/search/`、`/api/`、`/go/` 三段 Disallow）；② `gen-sitemap.mjs` 排除 `/search/` 与 `workbench.html`，sitemap 从 393→392 条，与 robots 一致。重建推产验证全绿。
- **待旺财手动**：① 百度搜索资源平台 + Google Search Console 提交 sitemap（`https://mokakit.com/sitemap-index.xml`）并加站点验证文件/meta；② 盯 certbot 续期（11-10 到期自动续）。

## MCP Server 公网 HTTPS 接入（战略核心，已做）
- 公网端点 `https://mokakit.com/mcp`（Streamable HTTP + JSON-RPC 2.0），已验通：带 token initialize/tools/list/tools/call 全绿；无 token → 401；GET /mcp → 405。
- **生产化硬约束**：服务器 Node v18（无 strip-types）且只部署 dist 无 src/。改用 esbuild 自包含打包：新增 `mcp/build.mjs`（`npm run mcp:build`）把 `server.mjs` + `src/lib/*.ts` 打成 `deploy/mcp/server.mjs`（target=node18），并抽 `deploy/mcp/catalog.json`（94 工具元数据）。server 启动优先读 catalog.json，彻底不依赖 src/ 与 TS 运行时。
- **安全边界**：`mokakit-mcp.service`（systemd，`/opt/mokakit-mcp/`）仅监听 127.0.0.1:18700；nginx `mokakit-ssl.conf` 加 `location /mcp` 反代公网 HTTPS；`server-setup.sh` 新加 `[8/9]` 段自动装服务并生成随机 `MCP_TOKEN` 写入 `/opt/mokakit-mcp/.env`（权限 600，不进 git）。
- **鉴权**：可选 Bearer token（设 `MCP_TOKEN` 才强制）。`server.mjs` 加 `API_VERSION=v1` 预留 versioning；GET `/` 健康检查暴露 apiVersion/tokenRequired。
- **踩坑①（构建）**：`build.mjs` 块注释里 `src/tools/*/meta.ts` 的 `*/` 提前闭合注释块 → 报语法错；改为 `src/tools 下各 meta.ts` 修复。
- **踩坑②（astro 增量）**：本次 build 偶发只产 1 个 html（112ms 退出，dist 残缺），是 shim 让删除变 no-op + 增量判定"无变更"跳过渲染；用 `mv dist dist_bak_xxx` 移走残缺目录逼全量重建解决（1.73s 渲染完整）。
- 提交 `e7e62ee`「feat(mcp): 公网 HTTPS 接入 MCP Server」（10 文件，含 deploy/mcp 产物 + nginx/setup/robots 改造）。
- **对外公开前待办**：① 写客户端接入文档（端点/协议/Bearer 获取）② 工具增改后 `npm run mcp:build` + 重部署 ③ breaking change 走 `/mcp/v2`。
