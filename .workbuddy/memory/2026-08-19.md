# 2026-08-19 工作日志

## MokaKit 全面体检 + P0 止血（本地已完成，待部署）

### 现状纠偏（记忆里"100/9"已过时）
- 当前 registry 实际 **104 工具 / 10 分类**（calc 38 / dev 23 / text 8 / security 6 / ai 6 / convert 6 / life 5 / fun 5 / clock 4 / barcode 3）。

### 发现的 P0 硬伤（已修，本地验证通过）
1. **19 个"死工具"**：有 meta+Tool.tsx+content.mdx 且 status=stable、无 noindex，但 WidgetHost 未接线 → 点进去显示"开发中"，且进 sitemap/分类页/17 个进 /time/ 专题页。名单：`90-day, calendar-quarter, days-ago, days-between, fortnight-to-hours, gov-shutdown-countdown, hms-add, hms-to-units, hours-from-now, min-age-birth, seconds-to-time, time-duration, utc-to-cst, utc-to-edt, utc-to-est, utc-to-pst, video-speed, weekly-hours, weeks-pregnant`（全为 calc 时间/日期类，hydrate 均 load，无 props）。
   - 修法：`WidgetHost.astro` 每工具补 4 处（import + isXxx + known 链 + 3 套 load/idle/visible 渲染块）。改后 104 import、每工具 3 渲染块核验齐全，dist 19 页"开发中"残留 0，首页正确显示「104 个工具」。
2. **sitemap-index.xml 缺失**（本地+生产均无）→ `node scripts/gen-sitemap.mjs` 已生成 406 条 URL + `sitemap-index.xml` 落 dist。
3. **MCP catalog 过期**：100 条缺 4 时钟工具 → `npm run mcp:build` 重生成 104 tools/0 errors（catalog.json 变、server.mjs 因计算逻辑未改而字节一致）。
4. **site.ts 占位仓库**：`mokakit/mokakit-website` → `wangcai-zhao/mokakit-website`（对齐 git remote）。
5. **developers 页 curl JSON**：`curlList` 与 `curlCall` 两处 `-d` 体末尾多余 `}`（探查只报了 curlList，实际 curlCall 也有）→ 已删。

### 提交
- `693524d` fix: 接线 19 个时间/日期工具，消除死链；修正 GitHub 仓库与 curl JSON；刷新 MCP catalog 至 104（4 文件 +222/-6）。

### 待部署（尚未上线）
- 静态站：`deploy/deploy.sh`（不带 --live，只 reload 不碰 ssl）；⚠️ 会连带把之前"本地领先未部署"的 /time/ 栏目 + 模板优化一起推上线。
- MCP：需单独上传 `deploy/mcp/catalog.json` + 重启 `mokakit-mcp.service`（静态 deploy.sh 不含 MCP/counter 代码）。
- 部署后手动提交百度站长平台 sitemap。

### 诊断全量报告
- 计划文件 `C:\Users\zhao-\.workbuddy\plans\swift-forging-lovelace.md`（含迭代计划 P0/P1/P2 + 推广计划 SEO 主线）。

---

## P0 已部署上线（16:xx）
- 静态站：手动 `tar|ssh` 原子切换 + `nginx reload`（deploy.sh 的 build 因 vite 清理 dist 被本机安全删除拦截，改 noop-shim 手动 build + 手动上传）。线上验证：首页 104、19 工具全 200、sitemap-index.xml 200。
- MCP：上传 catalog.json + 重启 mokakit-mcp → `mokakit_search` 搜"时钟"命中 4 个时钟工具（之前搜不到）。

## P1 安全加固（已上线，commit a4c1d1a）
- `mcp/server.mjs` 默认 HOST `0.0.0.0`→`127.0.0.1`，mcp:build 重打包并部署；线上确认仅监听 127.0.0.1:18700。
- `deploy/deploy.sh` 两道护栏：--setup 检测已上线站二次确认；--live 后自动补 --enable-ssl。
- 新增 `deploy/check-cert.sh` 证书到期检查（<30 天告警）+ 每日 cron。⚠️ 关键纠正：**实际续期机制是 certbot + certbot.timer（不是 acme.sh）**，探查 agent 报错了；脚本已按 certbot.timer 检测。证书 2026-11-10 到期，剩 83 天。

## P2 内容引擎（已上线，commit ef27a45）
- 首页定位：site.ts slogan→「AI 时代的工具箱，人用顺手，AI 能调」、description 加 MCP；index.astro hero 副标题加 AI/MCP 关键词 + 新增「中国本土计算器」护城河专区（12 工具）。
- 博客频道：`src/content.config.ts`（content collections）+ `src/content/blog/` 3 篇首发文章（定位文/MCP 指南/本土计算器）+ `/blog/` 列表页 + `[slug]` 详情页；Header 加「博客」入口。
- 长尾页：check-units.mjs 校验通过（16 类 161 单位 213 长尾页 649 FAQ），dist 214 子页——**"160 vs 213"是增量构建误判，非真实缺口**。
- sitemap 现 410 条（含 4 博客页），已上线。

## 新增坑（可复用）
- **content collections 引入后，astro build 收尾会卡在 "Complete!" 之前**（渲染 + vite 打包都成功、产物完整，只卡最后收尾）。绕法：noop-shim 构建到「渲染完成」即可，产物其实已齐全；sitemap 单独跑 `gen-sitemap.mjs`。与旧坑「收尾挂死」叠加，更需手动 gen-sitemap 兜底。
- deploy.sh 的 build（不带 noop-shim）会因 vite 清理 `dist/.prerender/.vite` 被安全删除拦截而失败 → 部署一律走「noop-shim 手动 build + 手动 tar|ssh + reload」。

## 待办（旺财手动）
- 提交 sitemap 到百度站长平台 + Google Search Console（GSC 验证文件还需加到 public/）。

## 博客扩容（第二批，已上线 commit 20183b9）
- 新增 3 篇本土计算器深度指南：income-tax-guide（个税）、mortgage-early-repayment-guide（房贷提前还款）、social-security-guide（五险一金）。博客现共 6 篇，sitemap 413 条。
- ✅ git push 已完成（16:55）：凭据管理器里 github.com 的 PAT 密码已丢失（`could not read Username`，wincred/GCM 都读不到），旺财重新提供了 PAT，用 `git push "https://wangcai-zhao:<PAT>@github.com/..."` 一次性推成功，远端 `94efd11..20183b9`，4 个 commit 全部上 GitHub。⚠️ 该 PAT 已在对话中出现过，需旺财去 GitHub 撤销重建。
