# 2026-08-22 工作日志

## 修复 /developers/ 与 /submit/ 错误联系邮箱（旺财反馈）
- **问题**：两页申请/投稿入口邮箱误用 `wangcai@mokakit.com`，但该 mokakit.com 域名邮箱实际未开通，邮件发过去无人收，等于申请通道是死的。旺财确认真实邮箱为 `bo.zhao2026@outlook.com`。
- **改动**（共 4 处）：
  - `src/pages/developers/index.astro`：mailto 定义（L6）+ 显示文本（L153）
  - `src/pages/submit/index.astro`：mailtoBase（L14）+ JS 跳转（L105）
  - 全部替换为 `bo.zhao2026@outlook.com`，旧邮箱零残留。
- **构建/上线**：上回合 build 实际已成功（dist 于 8/21 生成，415 页完整，含新邮箱），后台任务收尾被 tail 缓存误判卡死；本轮未重建，直接护栏校验（旧邮箱0/sitemap无example.com/新邮箱 developers2+submit3/415页）→ tar 上传 /var/www/mokakit（保留 .old）→ nginx reload 成功。
- **线上验证**：/developers/ 新邮箱 2 处、旧 0 处；/submit/ 新 3 处；状态码 200。全部通过。

## MCP Server 诊断（旺财试用 Token 前）
- 服务端**正常**：无 Token 返回 401、initialize 通过、catalog=104/可用 15 工具。
- **tools/list 须先 initialize 取 `Mcp-Session-Id` 再带该头**——这是 MCP 2025-06-18 协议要求，非 bug（旺财最初以为"申请 Token 有问题"部分源于此）。
- 现役生产 Token（server 单 Token 机制，写在 /opt/mokakit-mcp/.env）已交付旺财本地实测可用。后续做公开发号需改造为多 Token 机制。

## /developers/ 增加「调用顺序」提示块
- 旺财采纳建议：在 §2 鉴权段后加 `alert alert-warning` 提示块，强调所有请求必须先 `initialize` 取 `Mcp-Session-Id`，后续 `tools/list`/`tools/call` 必须带该头，否则报 `No valid sessionId`，避免后来人误以为服务坏了。
- 图标用确定存在的 `activity`（已确认 icons.ts 无 info/alert 类图标，缺失会回退 grid）。
- build 仍偶发 `EXIT=1` 但产物完整（校验 415 页 / 新提示已编译 / 新邮箱2 / 旧0），已上传 + nginx reload 上线；线上验证：调用顺序提示 1、新邮箱 2、旧邮箱 0、200。
