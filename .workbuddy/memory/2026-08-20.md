# 2026-08-20 工作日志

## MokaKit 公安备案链接修复（已上线）
- 背景：公安备案链接原用 `https://beian.gov.cn/portal/registerSystemInfo?recordcode=...`（旧格式），按旺财要求改为公安部官方代码格式。
- 改动：
  - `src/components/Footer.astro`：公安备案链接 → `https://beian.mps.gov.cn/#/query/webSearch?code=<纯数字>`（`rel="noreferrer noopener"`），并在文字前加官方备案图标 `<img src="/police-icon.png" width="16" height="16">`。
  - 新增 `public/police-icon.png`（来源：旺财提供的 `C:\Users\zhao-\Downloads\备案图标.png`，1.4 KB）。
- 构建：astro build 三连 Exit 1（vite 重优化偶发卡死老毛病），但产物完整（dist/index.html + dist/police-icon.png 均在，`beian.mps.gov.cn` 命中、旧链接 0 命中）——产物即真。
- 部署：deploy.sh 内嵌构建卡死，改用「tar 上传 + nginx reload」手动两步走（与 deploy.sh 非 --live 分支同逻辑），线上验证通过：`beian.mps.gov.cn/#/query/webSearch?code=11010502062390` + `police-icon.png` HTTP 200 + 旧链接 0 命中。旧版保留在服务器 /var/www/mokakit.old。
- git：commit 75c1957 已落本地；**push 失败——原 classic PAT 已失效（api.github.com 401）**，等待旺财重建 token（详见 MEMORY.md Git 段）。

## /developers/ 404 修复（同日）
- 旺财反馈 https://www.mokakit.com/developers/ 404。
- 根因：今天 build 连续 4 次 Exit 1（vite 重优化卡死），期间 `mv dist dist_stale_xxx` 逼全量重构建，但重构建产物不完整——**405 页、缺 developers/**（源码 src/pages/developers/index.astro 在）。这份残缺 dist 被上传上线 → 404。
- 修复：被 mv 走的完整版 dist_stale_1787236320 实为 **415 页完整产物**（含 developers + police-icon + mps 新链接），mv 回 dist 并校验（developers/police-icon/mps 链接/旧链接 0 残留）后重新 tar 上传 + nginx reload。
- 线上验证：/ 200、/developers/ 200、/developers 301（自动补斜杠）、/tools/ 200、/about/ 200；备案新链接仍在线。
- ⚠️ 教训：**Astro build Exit 1 时产物可能不完整（缺页），上传前必须校验 dist 关键页面（至少 index + developers + 页数）**；deploy.sh 内嵌构建卡死时，可直接用已校验的 dist 走「tar 上传 + reload」两步（与脚本非 --live 分支同逻辑）。
