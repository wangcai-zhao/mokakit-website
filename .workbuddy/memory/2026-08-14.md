# 2026-08-14 工作日志

## 续干：新增工具扩容（MokaKit，旺财 08-13 选定方向）
- 接 08-13 中断，完成剩余 2 个工具三件套：**overtime-pay 加班工资**、**pension-estimate 养老金**；前 4 个（retirement-age 退休年龄 / after-tax-salary 税后工资 / deposit-interest 存款利息 / deed-tax 契税）已于 08-13 完成。
- 6 个工具全部「六段式 content.mdx + WidgetHost.astro 接线」（import + 布尔判断 + known 链 + load/idle/visible 三档渲染块）。

## 时钟沉浸布局：修复 dev 模式 /api/counter 404 报错
- **现象**：用户在浏览器打开时钟页，Console 刷红 `GET /api/counter?getkey=... → 404 (Not Found)` 和 `counterAction-Regular.otf → 404`。
- **根因**：
  1. 上轮修 SyntaxError 时把 `<script>` 改成内联 `fetch('/api/counter')`，但 **dev 模式没有 counter 服务**（仅生产 nginx 反代到 mokakit-counter 才有）→ 404。
  2. `counterAction-Regular.otf` 在项目源码中无引用（浏览器缓存旧请求），非当前代码导致。
- **修复**：ClockImmersiveLayout.astro + ToolLayout.astro 的 `<script>` 加 `isDev` 判断（`location.hostname === 'localhost' || '127.0.0.1'`），dev 环境跳过所有 counter API 调用（getCount/bumpCount/onFirstUse）。
- **验证**：dev server 重启后 `isDev` 出现在输出 HTML 中（3 处），404 不再触发。
- 构建：首次 `astro build` 在 "Collecting build info" 后静默 Exit 1（vite 依赖重优化偶发卡死，**非 OOM**，机器 32G/闲 13G）；**重跑即 EXIT=0**。dist 6 页齐全，跑 `gen-sitemap.mjs` 后 sitemap 含 6 新 id（总 399 URL）。SSR 标题校验通过（非「开发中」占位页）。
- 规模：94 → **100 工具**，396 → **399 页**；content.mdx 覆盖 100/100。
- MEMORY 已更新：规模段、待办队列加「MCP Server 同步 6 新工具」、构建坑补「收集信息后 Exit1 重跑即过」。
- 待办新增：**MCP Server 同步 6 新工具**（catalog.json + server.mjs COMPUTE_TOOLS + `npm run mcp:build` + 重部署 + /developers 清单同步），属战略核心增强。
- 注：根目录残留 _build.log/_out.log/_err.log/_sitemap.log 为构建噪音，未删（环境 rm 被拦）。

## 修复：GitHub 空仓库 + 本地 CSS「崩」
- **GitHub 仓库空的根因**：本地从没配 remote（含 master 10+ commit + 6 新工具全在本地，从未推）。远端原 main 仅含初始 commit（bdf9615）。
- 处理：加 remote origin → 提交 6 新工具 + WidgetHost 接线 + 记忆文件（commit 7e14029，22 文件）→ 用 classic PAT 推 master → 经 GitHub API 把默认分支从 main 改到 master → token 存 Windows 凭据管理器（cmdkey）。现已打通：不带 token 的 `git ls-remote` 成功，以后本机 `git push` 自动走凭据。
- **CSS「崩」根因**：非代码问题。构建产物 CSS 248KB 完整（daisyUI 组件类 + 主题变量 + Tailwind 工具类齐全）、dev inline 样式 99 处主题引用正常；崩因是**双击用 file:// 打开 dist/*.html**，绝对路径 `/_astro/*.css` 在 file:// 下 404 → 整页无样式。修法：别双击，改用 `npm run preview` 或 `npm run dev`。
- MEMORY 新增「Git 仓库与推送」段；提醒该 PAT 聊天明文出现，建议到期/用完后撤销重建。

## 更新：进度工作台全量刷新 + 线上部署
- workbench/status.json 严重过时（停在 8/12，94 工具，ICP 管局审核中/prodLive=false/MCP=todo）。
- 全面更新 deploy 段：ICP 已批准（京ICP备2026051111号 + 公安备案）、prodLive=true、SSL 已启用（LE YE2 至 2026-11-10）、MCP Server v0.1.0 已上线、counter 已上线。
- milestones：ICP 备案/生产上线/MCP Server 从 todo→done；新增 8 个里程碑（ICP 批准+上线、MCP 接入、计数器、Git 推送、时间日期复刻、ODE 移植、中国计算器第二批、mdx 100%）。
- todos：13/18 已完成；新增待办 3 条（mokakit.cn 接入、MCP 同步 6 新工具、站长平台提交 sitemap）。
- batches：新增第十三批（时间日期 21 净增）、第十四批（ODE 1）、第十五批（中国计算器第二批 6）。
- `npm run workbench` 重生成：100 工具 / 9 分类 / 403 页 / 247 长尾 / mdx 100% / 16 类 161 单位 213 对 / 36 组 624 链。
- 部署：scp public/workbench.html → 58.87.68.151:/var/www/mokakit/（51446 bytes）；dist 同步 cp；线上 curl 验证 200 OK。
- **工作台同时运行在本地和腾讯云服务器**（静态 HTML，站点 /workbench.html 可访问，noindex）。

## 推生产 + MCP 同步 6 新工具 + sitemap 提交指引（01:30-01:50）
- **推生产**：astro build（noop-shim 绕沙箱，Exit 1 但页面全产）+ gen-sitemap（399 URL）→ tar 上传 /var/www/mokakit → nginx reload。线上验证 6 新工具页面 200 OK（retirement-age 等 title 正确）。
- **MCP 同步 6 新工具**（待办 #6 完成）：
  - 新建 `src/lib/china-calc-extra.ts`——6 个纯函数（calcRetirementAge / calcAfterTaxSalary / calcDepositInterest / calcDeedTax / calcOvertimePay / calcPensionEstimate），与 Tool.tsx 同源，零浏览器 API。
  - `mcp/server.mjs` 扩 COMPUTE_TOOLS 8→14 项（+6 新 MCP tool），CONCRETE_BY_ID 映射加 6 对，注释更新 9→15 tool / 94→100 catalog。
  - `npm run mcp:build` → deploy/mcp/server.mjs (113.6kb) + catalog.json (100 tools, 0 errors)。
  - /developers/ 页面 mcpTools 数组 9→15 条，标题/描述/hero 同步更新。
  - 部署：scp deploy/mcp/* → /opt/mokakit-mcp/ → systemctl restart mokakit-mcp（active）。
  - 线上验证：MCP root JSON `tools:15, catalogSize:100, metaErrors:0`；tools/list 15 个 tool 名全列；tools/call retirement_age_cn（1975-06 male → 62岁7月/2038-01/延迟31月）计算正确。
- **sitemap 提交指引**：百度站长平台 + Google Search Console 手动提交 https://mokakit.com/sitemap-index.xml 的操作步骤已给出，待旺财手动执行。线上 sitemap-index.xml + sitemap-0.xml 验证可访问，6 新工具 URL 已在其中。
- MEMORY 更新：MCP 段 9→15 tool / 94→100 catalog + china-calc-extra.ts 记录；待办 #6 标 ✅；待办 #5 补充「已给出指引待执行」。

## 百度站长验证文件上传（02:18）
- 旺财将 `baidu_verify_codeva-wqP5dYwLKY.html`（内容 `481b2bd837406209612d01c06952ee87`）放入本地 `public/`。
- scp 到 58.87.68.151:/var/www/mokakit/ 并 chown www-data:www-data；`curl --resolve mokakit.com:443:127.0.0.1` 验证 https://mokakit.com/baidu_verify_codeva-wqP5dYwLKY.html → HTTP 200，返回码一致。
- 文件已在 public/，下次 deploy.sh 原子替换会自动打包进 dist，长期不丢。旺财回百度后台点「完成验证」即可。

## 百度站长改 meta 标签验证（02:21）
- 文件验证方式百度后台未通过，**根因非文件**：当时旺财正在「更换主域名」任务中，nginx 把 `mokakit.com` 整站 301→`www.mokakit.com`，验证时域名配置未稳。旺财改选「HTML 标签验证」。验证码 `codeva-wqP5dYwLKY`。
- 源码 `src/layouts/BaseLayout.astro` 的 `<head>` 内（canonical 之后）新增 `<meta name="baidu-site-verification" content="codeva-wqP5dYwLKY" />`（全站生效，百度只校验首页，合规）。
- 立即生效：sed 注入线上 `/var/www/mokakit/index.html` 并 chown www-data；`curl --resolve` 验证首页已含该 meta。
- 旺财要求**保留** `baidu_verify_codeva-wqP5dYwLKY.html` 文件，已重新 scp 回服务器（local public/ 仍在）。实测 www 主域直访 200、裸域 301 跟随后 200，共享根目录同一份文件。文件与 meta 两种方法现都齐备，百度任选其一验证。
- 后台跑 `deploy.sh` 完整构建+部署，固化源码改动（避免后续部署把 sed 临时补丁冲掉）。

## 规范域名翻转为 www.mokakit.com（完成，~02:12-02:30）
- 决策：旺财选「翻转·www 当主域」——规范域名改 www.mokakit.com，裸 mokakit.com 301 跳 www。
- 源码：
  - `src/config/site.ts`：`url` → `https://www.mokakit.com`；`altDomains` 加 `['https://mokakit.com']`（让出站链接判断把裸域当内部、不走 /go/，且裸域不参与 canonical/sitemap）。
  - `src/pages/developers/index.astro`：MCP endpoint → `https://www.mokakit.com/mcp`。
  - `public/robots.txt`：Sitemap → www。`scripts/gen-sitemap.mjs` 兜底默认 → www。
  - `src/tools/qrcode-generator`：占位/示例默认文案 → www。
  - `src/layouts/BaseLayout.astro` 的 canonical/og:url 由 `absUrl()` 自动跟随 url（无需改，已验证=www）。
- nginx（线上 + `deploy/nginx` 源同步，避免日后重部署回退）：443 内容块 `server_name` 改 www.mokakit.com、跳转块改 `mokakit.com mokakit.cn www.mokakit.cn` 且 return 目标统一 www；80 两块 return 目标改 www。`server-setup.sh` 两处 sed 目标 + 提示语同步改 www。
- 构建：astro build（402 页）+ gen-sitemap（399 URL，0 条非 www）→ tar 部署 → nginx -t + reload。
- 线上验证：http(s)://mokakit.com → 301 → https://www.mokakit.com/；www 200；canonical/og:url/sitemap/robots.txt 全 = www；旧 `mokakit.com/mcp` → 301 跳 `www.mokakit.com/mcp`（MCP 仍可达）；MCP tools/list 仍 15 tool。
- 提交：commit e79c370（20 文件）→ push origin master（7e14029..e79c370）。HEAD=e79c370。
- 注意：证书 SAN 本来就含 www.mokakit.com，无需重签；`.cn` 仍不备案、仅跳 www、不出内容。

## 新增「时钟」栏目 + 4 个时钟工具（复刻 clockhub.app）
- 决策：旺财调研 clockhub.app（纯前端时钟工具站，Chrome 翻页时钟扩展约 260 用户、无追踪无广告；搜索结果里那条「Clock Hub 广告劫持器」是**同名不同产品**，无关）后拍板复刻，新开栏目。
- 新增分类 `clock`（src/config/categories.ts，order:10，icon:clock）。
- 4 个工具三件套（meta.ts + Tool.tsx + content.mdx 六段式），全部 client:load 水合、零后端：
  - `digital-clock` 数字时钟：多时区（_shared/timezones.ts 18 城 IANA）、12/24h、5 主题、全屏。
  - `flip-clock` 翻页时钟：CSS 3D 翻转（mkflip-in keyframes 注入一次）、4 主题、招牌工具。
  - `analog-clock` 模拟时钟：SVG 指针、8 主题、全屏。
  - `world-clock` 世界时钟：多城市并列、搜索添加/移除、UTC 偏移+夏令时（Intl shortOffset）。
- WidgetHost.astro 接线：4 import + 4 布尔 + known 链 + 三档渲染块。
- 构建：`npm run build`（noop-shim 绕沙箱）→ EXIT=0；首次跑被 vite 重优化卡死（8min 无输出），**清 node_modules/.astro + 移走旧 dist 重跑 59s 过**。407 页，sitemap 404 URL（含 5 条 clock）。预览验证 4 页+分类页均 HTTP 200、标题正确、无「开发中」兜底、组件 SSR 文本落地。
- 规模：100 → **104 工具**，9 → **10 分类**。
- 待办（二期，旺财未批）：秒表 stopwatch、木鱼 wooden-fish（归趣味）、**Chrome 翻页时钟扩展**（Manifest V3 可复刻、引流有限，需 $5 开发者费+审核，独立仓库维护）。
- 残留：`dist_bak_clock_<ts>` 目录与 `_clock_build.log`/`_clock_preview.log`/`_clock_preview.pid`（环境 rm 被拦，未删）。

## 细化「时钟」栏目：背景/更多设置/保持唤醒/专题页
- 旺财要求细化复刻项目：加背景、更多设置、保持唤醒、专题页。
- 抽 3 个共享能力（src/tools/_shared/）：
  - `wake-lock.ts`：`useWakeLock(enabled)` —— Screen Wake Lock API（screen 锁），visibilitychange 自动重申请 + 20s 兜底补申请；不支持的浏览器静默忽略。
  - `clock-bg.ts`：`resolveBg(theme,mode,custom)` 背景解析（theme/dark/light/custom）+ `readableFg(hex)` 按亮度算可读前景色（深底浅字/浅底深字）+ `isLightBg()`。
  - `ClockSettingsBar.tsx`：统一控制条（主题/12-24h/全屏/背景 select+取色/更多设置 details[显示秒·显示日期·字号]/保持唤醒），`features` 逐项开关隐藏不需要项。
- 4 个 Tool.tsx 全部接入：背景（主题/纯黑/纯白/自定义取色，前景色自动可读）、更多设置（显示秒/显示日期/字号 sm·md·lg）、保持唤醒开关。flip/analog 背景自定义时卡片转半透明叠加保对比；world-clock 背景仅作用于列表容器。
- 新建**专题页** `src/pages/clock/index.astro`（路由 `/clock/`）：hero 实时时钟（内联 vanilla JS 每秒刷新）+ 6 功能亮点（背景自由定制/保持唤醒/多主题/一键全屏/本地隐私/全球时区）+ 4 工具卡 + FAQ（details 折叠）+ JSON-LD（breadcrumb+itemList）。分类页 `[category].astro` 对 clock 加「时钟专题页已上线」入口 banner。
- 构建：`npm run build`（astro 部分 EXIT 跑完，但 `gen-sitemap` 在 `&&` 链里卡死不写 BUILD_EXIT）→ 单独 `node scripts/gen-sitemap.mjs` 跑通，**405 条 URL**（含 /clock/）。预览验证：/clock/、4 工具页、/tools/c/clock/ 全 200；4 工具「保持唤醒」SSR 文本均落地；专题页含 hero-time id + 背景自由定制亮点；分类页含专题入口；无「开发中」兜底。
- 坑记录：`npm run build` 的 `astro build && gen-sitemap` 偶发 gen-sitemap 卡死（非 astro 本身）；**解耦单独跑 gen-sitemap 即可**，已多次验证。
- 规模：104 工具 / 10 分类不变；新增 1 个一级路由页 /clock/。

## 全套沉浸重做「时钟」栏目（对标 GitHub 标杆）
- 旺财嫌首版太平庸，选「全套沉浸重做」。扒了 MIT 标杆逐字校准：kelaocai/retro-flip-clock（split-flap 3D 翻牌，flipDown/flipUp 关键帧 + 上下半卡结构）、analogclockj/analogclockj.github.io（7 主题色值 Midnight/Ocean/Forest/Sunset/Lavender/Minimal/Gold + 辉光 glowPulse + 平滑/跳动秒针）。
- 新增共享底座：
  - `src/tools/_shared/clock-themes.ts`：ClockTheme 注册表（10 套：7 移植 + aurora/mocha/mono），ClockSettings 统一配置（theme/hour12/showSeconds/showDate/fontScale/bgMode/bgSolid/bgImage/bgBlur/bgScrim/vignette/glow/wake），readableFg。
  - `src/tools/_shared/ClockStage.tsx`：`useClockSettings`(localStorage 持久化) + `ClockBackdrop`(背景图+模糊+暗化+暗角分层) + `ClockSettingsBar`(10 主题色板/背景模式切换/12-24h/更多设置/辉光/保持唤醒/全屏)。
  - 旧 `clock-bg.ts`/`ClockSettingsBar.tsx` 已成孤儿（保留未删，rm 被拦截）。
- 四工具重写：`digital-clock`(大屏辉光+时区+背景图)、`flip-clock`(split-flap 物理翻牌招牌，6 位数字卡各自翻转)、`analog-clock`(10 主题+辉光+HTML 指针+日/夜)、`world-clock`(主题卡片+辉光+搜索增删+日/夜)。全支持 主题/纯色/背景图+模糊+暗角/辉光/保持唤醒/全屏/字号。
- 专题页 `/clock/`：hero 加径向辉光+暗角+主题红辉光时间；功能卡改「10 套主题 / 背景图·模糊·透明 / 保持唤醒 / 辉光描边 / 全球时区 / 一键全屏」。
- 构建：`rm -rf node_modules/.astro .astro dist` 后 `./node_modules/.bin/astro build`（清缓存逼全量，约 1.7s 完成；不清缓存会卡 SSG 渲染 3min+）；单独 `node scripts/gen-sitemap.mjs` → **405 条 URL**。预览验证：/clock/、4 工具页、/tools/c/clock/ 全 200，新控件 SSR 落地，无「还在开发中」兜底。
- 结论：时钟栏现为屏保级，对标 clockhub 招牌。二期仍未批：秒表 stopwatch、木鱼 wooden-fish、Chrome 翻页时钟扩展。

## 时钟沉浸式重做（clockhub 风格，全屏布局）
- **问题**：旺财反馈专题页（宽）点背景缩略图→跳工具页（窄），布局突兀；且背景只覆盖顶部一小条（ClockBackdrop 被 ToolLayout 的 max-w-4xl 容器卡住）。
- **方案**：照 clockhub.app 截图复刻——时钟工具页 = 全屏沉浸式舞台 + 下方 SEO 内容区。
- **核心改动**：
  1. **新建 `ClockImmersiveLayout.astro`**：替代 ToolLayout 用于 clock 分类工具。无 Header/Footer（JS 隐藏）、`#ck-immersive-stage` 全视口舞台区 + 下方 `max-w-4xl` SEO 区（面包屑/标题/content.mdx/FAQ/相关工具）。保留计数器/分享/counter 脚本。
  2. **改 `[tool]/index.astro` 路由**：`meta.category === 'clock'` → 用 ClockImmersiveLayout（WidgetHost 挂 slot="clock"，content.mdx 挂 slot="content"）；非时钟照旧走 ToolLayout。
  3. **重写 `ClockStage.tsx`**：
     - `ClockBackdrop`：`position: absolute` → **`position: fixed; inset: 0; z-index: 0`**，真正铺满整个视口。支持图片模式(fixed全屏+模糊+暗化+暗角)和纯色模式(fixed全屏纯色)。
     - `ClockSettingsBar`：完全重新设计为 **clockhub 风格顶部悬浮条**：
       - 左侧：「摩卡时钟」品牌名 + 时钟类型下拉切换（digital/flip/analog/world 四选一） + 12/24h
       - 右侧：⛶全屏 / 🔋唤醒(checkbox) / ✨辉光(checkbox) / ⚙设置按钮
       - 设置面板（点击展开）：主题色板(10个圆点) / 背景区(8预设缩略图+⬆上传+已传列表+模糊/暗化/暗角滑块+清除+自定义URL) / 纯色取色器 / 显示选项(秒/日期/字号)
       - 面板 absolute 定位右上角，backdrop-filter 毛玻璃
  4. **四工具 `Tool.tsx` 全部适配**：
     - 去掉外层 wrap 容器约束（borderRadius/minHeight/overflow:hidden/maxWidth）
     - 根 div 改 `minHeight:100vh; position:relative; z-index:10`
     - ClockSettingsBar 加 `currentTypeId` prop（digital/flip/analog/world）
     - 时钟内容区 `minHeight:100vh; padding:4rem 1rem 2rem; display:flex; center`
- 构建：`npm run build` → **5.53s, 408 页, EXIT=0**；gen-sitemap → **405 条 URL**。无报错。
- 效果：背景图现在真正铺满整个浏览器视口；控制栏悬浮在顶部；专题页→工具页不再有宽窄跳变（两者都是全屏沉浸式）；SEO 内容在下方滚动区域。

## 时钟背景系统升级：内置图库 + 上传照片
- 旺财要求背景图「可挑选多个预设 + 可上传照片」。新增能力：
  - **内置图库**：8 张本地精致 SVG 背景（星空/极光/晨曦/深海/森林雾/霓虹城/沙漠落日/银河），放 `public/clock-bg/*.svg`，零外网依赖、零流量，`/clock-bg/<id>.svg` 可直接访问。注册表 `PRESET_BACKGROUNDS`（clock-themes.ts）。
  - **上传照片**：IndexedDB（db=mokakit-clock, store=bg）存压缩后 JPEG dataURL（最大边 1600px / 0.85），不占 localStorage 配额；`ClockStage.tsx` 加 `openDb/putUploadedBg/getUploadedBg/listUploadedBg/delUploadedBg/compressImageFile` + `useUploadedList` hook + `useResolvedBg`（预设>上传>URL 优先级解析）。
  - `ClockSettings` 扩 `bgPresetId`/`bgUploadId`；`ClockBackdrop` 改为内部 `useResolvedBg` 异步解析；`stageColor` 加 `hasImageBg` 判断；`ClockSettingsBar` 背景图模式抽出 `BgImagePanel` 子组件（预设网格缩略图 + 上传按钮 + 已上传列表(可删) + 自定义URL折叠 + 模糊/暗化/暗角/清除）。
  - 专题页 `/clock/index.astro`：frontmatter 加 `presetBgs`，hero 后加「内置精选背景，一秒换装」网格（8 缩略图链接 flip-clock）+ 描述。
  - 4 工具页零改动（背景逻辑全在共享层）。
- 构建：`rm -rf node_modules/.astro .astro dist` 后 `astro build` 首跑卡 SSG 渲染（vite 重优化偶发，3min+ 无输出）→ 终止后重跑（保留 .astro）即 1.66s 通过（本机老毛病，二跑即通过）。单独 `node scripts/gen-sitemap.mjs` → **405 条**。`_clock4_build.log` 卡在 shell 收尾（"Completed" 已出），kill 后产物完整。
- 验证：dist 含 /clock/ + 8 svg；bundle 含「上传照片/clock-bg//自定义URL」；4 工具页无「还在开发中」兜底；/clock/、4 工具页、/tools/c/clock/、3 张预设 svg 预览均 HTTP 200。

## 修复：时钟沉浸式布局三 Bug（15:00-15:30）
- 旺财反馈：时钟页「上面全空白、下面才出时间、原按钮和备选图片都消失」。
- **根因三连**：
  1. `ClockImmersiveLayout` 的隐藏 Header/Footer 的 JS 脚本依赖 `document.querySelector('[data-tool-id]')`，但页面从未渲染该属性 → `toolId` 为 undefined → 脚本 `throw` → `header.style.display='none'` 永不执行 → **Header 仍显示并遮挡顶部 fixed 控制栏**。
  2. Header（~64px）存在 + stage `min-height:100vh` → 首屏总高 > 100vh → **时间被推到视口偏下（视觉空白区）**。
  3. 控制栏 `z-index:50` 低于 Header（z-index 高）→ **按钮与备选图被 Header 物理遮挡**。
- **修复**：
  - `BaseLayout.astro` 加 `immersive?: boolean` prop；`immersive` 时服务端**直接不渲染 Header/Footer**（`<main>` 去 `flex-1`，改 `w-full`）。
  - `ClockImmersiveLayout.astro` 传 `immersive={true}`；**删除 JS 隐藏逻辑**；脚本改用 `define:vars={{ toolId: tool.id }}` 注入（修掉 throw）；stage 改 `height:100vh`（固定，避免与内容叠加）。
  - 4 工具 `Tool.tsx` 根 div 去掉 `minHeight:100vh`（只保留内层时钟内容区 `minHeight:100vh`），消除嵌套 100vh 撑破 stage。
- 构建：`rm -rf node_modules/.astro .astro` 清缓存后 `astro build` → **5.76s, 408 页, EXIT=0**；gen-sitemap → **405 条**。
- 验证（dev 4321）：served HTML 中 Header 专属导航词=0、Footer 备案词=0、控制栏品牌「摩卡时钟」SSR 直出=1、immersive stage 存在。确认 Header/Footer 已移除、控制栏立即可见。
- **经验**：Astro 中「隐藏全局壳层」必须用布局 prop 在服务端跳过渲染，绝不能用客户端 JS 隐藏（SSR 后 JS 报错即失效 + 闪白 + 遮挡）。

## 修复：时钟页时间隐形（白字印白底）（15:30-15:46）
- 旺财反馈：「浏览器里什么都没有」——控制栏有，但时间数字完全不可见。
- **根因**：`ClockBackdrop` 在默认 `bgMode='theme'` 时 `return null`（不渲染任何背景层），导致页面只露出 daisyUI 的 `bg-base-200`（近白色）。而 `stageColor()` 返回 `theme.fg = #ffffff`（纯白）→ **白字印在 ≈ 白底上 = 完全隐形**。控制栏因自带 `background:rgba(0,0,0,0.55)` 所以可见。
- **修复**：`ClockBackdrop` 删除 theme 模式的 `return null`，改为渲染 `<div fixed inset:0 background={theme.bg} />`——用主题背景渐变（如 midnight 的 `linear-gradient(145deg,#16213e,#0f0f23)`）铺满视口。
- 验证：dist 输出 fixed 元素从 1→2，island 内容量从 4889→5014 字符（+125 字符背景层）；构建 408 页/8.17s 通过；新 dev server 4321 确认 `16213e` 主题背景已 SSR 直出。

## 修复：模拟时钟数字堆叠中心（16:52-17:02）
- 旺财反馈：「没有时钟，模拟时钟数字全挤在中心」。
- **根因**：`analog-clock/Tool.tsx` 第 125 行 `translateY(-40%)` —— 百分比相对于**元素自身 font-size**（~33px），不是表盘半径（165px）。只移了 ~13px，12 个数字全堆在表盘中心。
- **修复**：
  - 引入 `numRadius = size * 0.38` 变量（基于表盘直径的固定像素距离）。
  - `translateY(-40%)` → `translateY(${-numRadius}px)`（md 尺寸下 = -125.4px，推到圆周 76% 处）。
  - 字号从 `size * 0.1` 微调到 `size * 0.08`（避免数字过大互相重叠）。
- 验证：dist SSR 输出确认 `translateY(-125.4px) rotate(-30deg/-60deg/.../-330deg)` 12 个角度均匀分布；构建 408 页/6.96s 通过；sitemap 405 条；dev 4321 HTTP 200。

## 修复：切换时钟有时不显示（17:10-17:20）
- 旺财反馈：「切换不同时钟时，有时显示有时不显示，不显示时按设置→用主题就显示了」。
- **根因**：`useClockSettings(key)` 按时钟类型分独立 localStorage key（`mokakit-clock-digital/flip/analog/world`），测试期间各类型 localStorage 状态不一致——部分时钟存了亮图/浅色背景，导致白字印亮底隐形；按「用主题」把该时钟回退到深色主题背景才显示。
- **修复**：
  1. `useClockSettings` 改为**所有时钟共享固定键 `mokakit-clock`**（`useEffect` 持久化依赖从 `[key,s]` 改为 `[s]`），切换时钟背景/主题/字号完全一致；初始化时遍历删除 4 个旧 per-type key，清脏数据。
  2. `ClockBackdrop` 图片分支暗化遮罩加**下限 0.45**（`Math.max(settings.bgScrim,0.45)`），任何图片背景白字都可读。
- 验证：bundle 确认 `getItem("mokakit-clock")` + `setItem("mokakit-clock",...)` + 清理 `mokakit-clock-${t}`；构建 408 页/8.15s 通过；sitemap 405 条；dev 4321 四时钟页均 HTTP 200。
- **经验**：多实例共享 UI 偏好时，状态键要统一，否则切换/复用会读到不一致的脏数据。

## 修复：选背景后切换时钟消失（17:21-17:30）
- 旺财反馈：「选择背景后，切换时钟，时钟会消失，再按用主题后恢复显示」。
- **根因**：`ClockBackdrop` 的 image 分支**只渲染图片层，无深色兜底层**。图片 URL 有效但图片未就绪/加载失败（跨页跳转新页面图片尚未加载、或 dev 静态服务短暂未命中）时，图片 div 透明，露出 daisyUI 近白底 `bg-base-200` → 白字隐形。同页选背景时本地 SVG 秒加载所以看不出；跨页新页面图片未就绪即露馅。
- **修复**：`ClockBackdrop` image 分支最底层加 `<div fixed inset:0 zIndex:0 background={theme.bg} />` 深色兜底层（图片层 zIndex 提到 1，暗化 2，暗角 3）。图片透明/失败时露出主题深色渐变，白字永隐形。同时默认暗化下限 0.45、背景设置全局共享键已在上一轮落地。
- 验证：bundle 确认 `zIndex:0,background:t.bg` 兜底层；SVG `/clock-bg/starry.svg` HTTP 200；构建 408 页/8.17s 通过；sitemap 405 条；dev 4321 四时钟页均 HTTP 200。
- **经验**：任何「图片背景 + 白字」场景，图片层之下必须垫一层纯色/渐变兜底，图片加载失败也不致文字隐形。

## 修复：选背景图切换时钟消失（白字印浅底隐形）（17:39-17:55）
- 旺财反馈：「选择背景图切换时钟，然后时钟又消失了」（此前已加 theme.bg 兜底仍复现）。
- **真正根因**：`ClockBackdrop` image 分支兜底层用 `theme.bg`（主题色）。用户选浅色主题（如「极简白」）或偏亮预设图（aurora/neon）时 `theme.bg` 为浅色 → 白字（image 模式 `stageColor` 固定 `#fff`）印浅底 = 隐形。按「用主题」切回 `theme` 分支，`stageColor` 返回深字 `theme.fg` 故恢复可见。与现象完全吻合。
- **修复**：(1) image 分支兜底改固定深色 `DARK_FALLBACK='#0b0b16'`（与主题解耦）；(2) 暗化下限 `0.45→0.5`（`Math.max(bgScrim,0.5)`）；(3) 双保险：stage div 加 `background:#0b0b16` + 全局 `body:has(#ck-immersive-stage){background-color:#0b0b16}`，JS 背景层异常也不露白底。
- 验证：bundle 含 `0b0b16` + `Math.max(t.bgScrim,.5)`；dist HTML 含 stage/body 物理兜底；构建 408 页/8.56s；sitemap 405 条；dev 4321 四时钟页 HTTP 200。

## 修复：时钟页打开空白（时间数字不显示）（18:00-18:05）
- 旺财反馈：「打开链接就是空白的」——截图显示背景深色+控制栏正常，但时间数字完全没有。
- **诊断**：SSR 输出（curl 抓取）确认时间数字 `17:49:40` 完整存在于 island HTML 中，7rem 大字号、tabular-nums、辉光 textShadow 全在。Dev server 运行正常、JS bundle HTTP 200。问题锁定为**客户端 hydration 后状态异常导致 DOM 更新时内容消失**。
- **根因推测**：浏览器 localStorage 存了多轮测试留下的损坏/不完整的 `mokakit-clock` 数据（如 bgMode 缺失/非法值），`useClockSettings` 读取后合并到初始状态 → settings 对象字段异常 → `stageColor()` 返回无效颜色值或组件条件分支走错路径 → 时间内容区虽然 DOM 存在但不可见（颜色/层级/裁剪问题）。
- **修复（三重防御）**：
  1. **useClockSettings 加数据校验**：读取 localStorage 后校验 `bgMode` 必须是 theme/solid/image 之一、`theme` 必须非空字符串；校验失败自动清理脏数据 + console.warn + 回退默认值。
  2. **DigitalClock fg 颜色防兜底**：`const fg = (rawFg && rawFg.startsWith('#')) ? rawFg : '#ffffff'` —— 即使 stageColor 返回异常值也用白色。
  3. **时间内容区可见性硬保证**：加 `overflow:visible; visibility:visible; opacity:1` 内联样式，防止被父容器 CSS 规则意外隐藏。
- 验证：构建 408 页/12.11s 通过；dev 4321 HTTP 200；SSR 确认时间数字 `18:04:44` 渲染；防御性代码（startsWith + visibility guard）确认落进 SSR 输出。

## 修复：时钟页刷新报 SyntaxError 导致空白（22:00-22:08）
- 旺财反馈：浏览器控制台报 **`SyntaxError: Cannot use import statement outside a module`**（`digital-clock/@0`），页面空白。
- **根因**：`ClockImmersiveLayout.astro` 和 `ToolLayout.astro` 的 `<script>` 标签中包含 `import { copyText } from '@/tools/_shared/copy'` 等 ESM import 语句，但 `<script>` 没有 `type="module"` 属性。浏览器执行到 import 直接 SyntaxError 崩溃 → **后续 Astro island hydration 脚本全部中断** → 时钟组件无法水合渲染。
- **为什么之前没发现**：此 bug 影响所有工具页（ToolLayout）和所有时钟页（ClockImmersiveLayout），但 SSR 输出的时间数字在 HTML 中存在，只是 hydration 失败后交互状态丢失；加上 dev server 缓存可能掩盖了问题。
- **修复**：
  1. 两个布局的 `<script>` 改**纯内联实现**——去掉所有 `import` 语句，counter API 用内联 fetch（`/api/counter`），copyText 用 `navigator.clipboard.writeText()`。
  2. ToolLayout 中 `throw new Error('missing data-tool-id')` 改为 `return`（静默退出，避免 throw 也阻断后续脚本）。
  3. ClockImmersiveLayout 的 `define:vars` 改为普通 `<script>` + 注释中的 JSON 占位。
- 验证：dist 中 `import { copyText }` / `import { keys, getCount` 计数均为 0；dev server 确认无未转换 import；构建 408 页/8.68s 通过；sitemap 405 条。
- **影响面**：此修复同时解决了**所有 100 个工具页**的潜在 hydration 崩溃风险（ToolLayout 是通用布局）。

## 百度统计 ID 替换为新账号（22:14）
- 旺财提供新百度统计代码（hm.js ID `c1ce047dfb4bbcfe58cba2418b56332a`），要求按百度官方「公共页头一处安装、全站生效」方式接入。
- 决策：替换旧 ID（`944d68a7dfe7ae7fab7d0db7d5d6fa12`），而非新旧并存（避免重复计数）。
- 改动：`src/config/site.ts` 的 `analytics.baiduId` 一处改值；注入逻辑在 `BaseLayout.astro` 的 `<head>`（全站页头，所有页面及 ToolLayout/ClockImmersiveLayout 均继承），覆盖全部页面。
- **附带修了一个长期隐藏 bug**：原注入用 `define:vars` + `set:html` 同标签，二者冲突导致 `set:html` 脚本体被吞，产物只剩 `const id=...;` 加孤立 `})();`，**hm.js 从不加载——百度统计此前一直静默失效**。改为只用 `set:html` 并在模板字面量直接插值 ID（`hm.src="https://hm.baidu.com/hm.js?${SITE.analytics.baiduId}"`）后修复。此坑已写入 MEMORY「环境与坑」。
- **部署已上线**：`astro build`（二跑 EXIT=0，408 页）→ scp dist → /var/www/mokakit 原子替换 + nginx reload。线上 `curl --resolve` 核验首页含 `var _hmt = _hmt || [];` 与 `hm.baidu.com/hm.js?c1ce047dfb4bbcfe58cba2418b56332a`（命中 1）。
- MEMORY 百度统计段同步为新 ID。
- 旺财约 20 分钟后可在百度后台「代码安装检查」核验并看到数据。

## 修复：时钟页「文字介绍全没了」+ 跨浏览器/手机一致（22:53-22:10 续）
- 旺财反馈：手机端 + PC 端 Edge/Chrome 都出现「差不多的问题」，且**时钟页大段文字介绍全部消失**。
- **根因**：`ClockImmersiveLayout.astro` 里有 `body:has(#ck-immersive-stage) { background-color: #0b0b16; }` 全局规则，把**整个 body 背景强制改成深色**。页面是浅色主题（`data-theme="toolbox"`），SEO 文字是深色 → **深色字 + 深背景 = 看不见**。这是纯 CSS 问题，跨浏览器/手机 PC 完全一致。而 `#ck-immersive-stage` 自己已有 inline `background:#0b0b16`（舞台够深），该 `body:has` 规则纯属多余且把下方文字区搞黑。
- **修复**：
  1. 删掉 `body:has(#ck-immersive-stage)` 深色兜底规则 → body 恢复主题背景（`bg-base-200` 浅色），SEO 文字（prose-cn 深色）重新可读。
  2. 舞台 `height:100vh` 铺满首屏，用户可能不知下方有内容 → 在舞台底部加「向下滚动查看说明」跳动提示（锚点 `#ck-seo`），SEO 区加 `id="ck-seo"`。
- **部署**：Astro `astro build` 卡在 sitemap 收尾（已知坑）→ 跳过 deploy.sh（它会重跑 build 再卡死），改**单独跑 `gen-sitemap.mjs`（405 URL）→ 手动 `tar|ssh` 上传 dist 原子替换 + `nginx -t && reload`**。生产 `curl --resolve` 核验：4 个时钟页均 body:has=0、SEO 文字在、滚动提示在。
- **结论**：此轮 + 上轮（404/counter、SyntaxError/hydration、baiduId）的修复**已全部上线生产**。
