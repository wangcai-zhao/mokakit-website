# MokaKit / 摩卡工具箱 · 项目跟进（2026-08-04 早 9 点）

> 旺财先生早上好，财运亨通，八方来财 💰

## 一、当前状态快照

- **技术栈**：Astro + Preact + daisyUI（自定义 `toolbox` / `toolboxdark` 双主题，亮/暗自动切换）
- **已落地工具（7 个，全部本地运行）**：
  密码生成、单位换算、时间戳转换、JSON 格式化、大小写转换、进制转换、哈希计算
- **部署资产就绪**（`deploy/`）：
  - `deploy.sh` 一键部署（构建 → 上传 → 原子切换，旧版留 `/var/www/mokakit.old` 可回滚）
  - `server-setup.sh` 服务器初始化（只读体检 OpenClaw + 装 Nginx + 上占位页）
  - Nginx 配置：`nginx/mokakit.conf`（正式）、`nginx/icp-pending.conf`（备案期占位）、`nginx/snippets/mokakit-headers.conf`
  - 占位页：`icp-pending/index.html`
- **共存方案**：与 OpenClaw 同机运行，**不停止、不改动 OpenClaw**（只占用 80/443，OpenClaw 网关端口 18789 不受影响）
- **部署状态**：仅本地预览，**尚未上腾讯云服务器**
- **品牌配置**：`SITE.name = 'MokaKit'`，`nameCn = '摩卡工具箱'`（ICP 备案网站名已预填中文名）
- **域名**：`mokakit.com`（主）+ `mokakit.cn`（301 跳转备用）；`icp` 字段仍为空
- **配色**：摩卡深棕主色（`oklch(47% 0.072 55)`）、焦糖奶咖辅色、蜜桃粉点缀，已应用到全站但此前未明确确认

## 二、待旺财先生拍板的 4 件事

1. **腾讯云轻量服务器公网 IP 拿到了吗？**
   拿到后我执行 `bash deploy/deploy.sh root@IP --setup`（先上备案占位页），随后引导你放通防火墙 80/443、配域名 A 记录、提交 ICP 备案（网站名填「摩卡工具箱」），备案通过再 `--live`。

2. **`/sites` 导航板块是否要做？品牌是否正式统一为 MokaKit？**
   当前导航只有「全部工具 + 4 个分类」，**没有 `/sites` 板块**；配置里品牌已统一为 MokaKit，待你最终拍板。

3. **摩卡配色是否满意？**
   此前沿用未明确确认，这次确认一下要不要定稿。

4. **是否需要继续铺第三批工具？**
   现有 7 个为一/二批成果，第三批可按你的想法继续。

## 三、顺手发现的小问题（非阻塞）

- `SITE.description` 文案里写了「简繁转换、汇率换算」，但这两个工具目前还没做（7 个工具里没有）。建议等工具补齐，或先改文案，避免 SEO 描述与站内内容不符。

## 四、部署命令（拿到 IP 后直接可用）

```bash
bash deploy/deploy.sh root@<你的公网IP> --setup
```

后续流程：放通防火墙 80/443 → 域名 A 记录指向该 IP → 提交 ICP 备案（网站名「摩卡工具箱」）→ 备案通过后执行 `bash deploy/deploy.sh root@IP --live`。
